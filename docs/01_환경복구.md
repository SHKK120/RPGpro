# 환경 복구

## 문서의 역할

프로젝트를 시작하거나 개발 환경을 복구할 때 필요한 절차를 관리한다.

## 이 문서가 소유하는 정보

- 필수 도구와 버전 요구 사항
- 프로젝트 실행 및 복구 절차
- 환경 복구 후 기계적으로 확인할 항목

## 이 문서가 소유하지 않는 정보

- 게임 플레이 규칙
- 콘텐츠와 수치
- 기능별 구현 순서

## 현재 확정된 내용

### 확인된 환경 정보

- [확인 사실/직접 확인] 프로젝트 경로: `C:\Users\CKIRUser\RPGpro`
- [확인 사실/직접 확인] Git 저장소: 있음, 저장소 루트 `C:\Users\CKIRUser\RPGpro`
- [확인 사실/직접 확인] 현재 브랜치: `main`
- [확인 사실/직접 확인] remote 이름: `origin`, 공개 저장소 주소: `https://github.com/SHKK120/RPGpro.git` (인증 정보는 기록하지 않음)
- [확인 사실/직접 확인] Godot 실행 파일: `C:\godot\godot.exe`
- [확인 사실/직접 확인] Godot 버전: `4.7.2.stable.official.ed1daf0bf`
- [확인 사실/직접 확인] `project.godot`: P0에서 저장소 루트에 생성
- [P0 시험 선택] 렌더러: Compatibility (`gl_compatibility`). 첫 실행 확인용이며 게임의 영구 렌더러 결정이 아니다.
- [확인 사실/직접 확인] P0 CLI import와 메인 씬 headless 로드: 오류 없이 완료
- [확인 사실/직접 확인] P0 일반 그래픽 렌더: Compatibility가 Intel HD Graphics 530에서 ANGLE로 실행되어 1280×720 프레임 출력 완료. 드라이버 품질 경고는 별도 기록함.
- [미확인] 사람이 에디터에서 F5/F8 후 닫았다 다시 열어 재실행하는 확인
- [확인 사실/전달된 사용자 보고] Codex Beta에서 접속하여 문서 작업을 진행한 상태
- [미확인] Codex의 정확한 제품명, 빌드, 선택 방법 및 초기화 후 재현 여부
- [미정] 한 세션 가용 시간. 다른 프로젝트의 30분 제약을 이 프로젝트에 적용하지 않는다.

### 검증된 시각 제작 환경

2026-09-06에 실제 실행 파일·설정·무결성을 다시 확인한 현재 로컬 상태다. 아래 도구와 설정은 아직 원격 보존됐다고 간주하지 않는다.

| 항목 | 현재 경로·버전 | 확인 상태 |
|---|---|---|
| Godot | `C:\godot\godot.exe` — `4.7.2.stable.official.ed1daf0bf` | `--version`과 현재 프로젝트의 headless editor 로드 성공 |
| Blender Portable | `C:\Users\CKIRUser\Tools\Blender\blender-4.5.13-windows-x64\blender.exe` — Blender `4.5.13 LTS`, build `daeeeca98fb0` | `--version` 성공. 실행 파일 옆 `portable` 설정 사용 |
| uv / uvx | `C:\Users\CKIRUser\Tools\uv\0.12.10\uv.exe`, `uvx.exe` — uv `0.12.10` | 실제 실행과 버전 확인 |
| MCP용 Python | `C:\Users\CKIRUser\Tools\BlenderMCP\python\cpython-3.11.16-windows-x86_64-none\python.exe` | Python `3.11.16` 실행 확인 |
| Blender MCP | PyPI `blender-mcp==1.9.1` | 로컬 패키지 메타데이터의 이름·버전과 프로젝트 등록의 고정 버전 확인 |
| RPGpro 프로젝트 Skill | `.agents/skills/rpgpro-visual-slice/`, `rpgpro-lowpoly-modeling/`, `rpgpro-material-texture/`, `rpgpro-2d5d-asset/` | MASTER-03R 종료 검사에서 네 폴더를 공식 validator로 확인 |

Blender Portable 애드온은 `C:\Users\CKIRUser\Tools\Blender\blender-4.5.13-windows-x64\portable\scripts\addons\blender_mcp.py`에 있다. 현재 파일과 보관된 `blender-mcp==1.9.1` bundled `addon.py`는 크기 `175,194 bytes`, SHA256 `f43469c8518c7021e0060e32cfe52e3beb126b0f62fbae7293106642a3ebda89`로 일치한다.

### Codex 프로젝트 범위 Blender MCP

- 설정 파일: `C:\Users\CKIRUser\RPGpro\.codex\config.toml`
- 서버 이름: `rpgpro_blender_b1`
- 실행 명령: 위 절대 경로의 `uvx.exe`가 Python `3.11`과 `blender-mcp==1.9.1`을 사용한다.
- 작업 폴더: `C:\Users\CKIRUser\BlenderTrials\B1`
- 연결: `127.0.0.1:9876` 루프백만 사용한다.
- 안전 설정: `BLENDER_MCP_SAFE_MODE=1`, `DISABLE_TELEMETRY=true`, 기본 및 개별 도구 승인 모드 `prompt`.
- 상시 허용 관찰 도구: `get_scene_info`, `get_object_info`, `get_viewport_screenshot`.
- `execute_blender_code`는 상시 허용하지 않는다. 승인된 Blender 제작 조각에서 재현 코드를 먼저 저장하고 호출별 승인을 유지한 채 일시적으로만 열며, 완료 후 관찰 목록으로 복원한다.
- Poly Haven, Sketchfab, Poly Pizza, Hyper3D, Hunyuan3D와 외부 생성 서비스는 현재 복구 기본값에 포함하지 않는다. 사용·비용·라이선스·다운로드·원본 보관을 별도 승인한 조각에서만 검토한다.

현재 설정 TOML은 실제 파싱에 성공했고 위 세 관찰 도구와 각 `prompt` 항목이 존재한다. 다만 이번 Codex 세션은 설정 변경 전의 두 조회 도구만 노출했다. `get_viewport_screenshot`의 실제 노출은 새 Codex 작업 또는 MCP 재시작 뒤 확인해야 한다.

이번 확인 시 `get_scene_info` 도구 호출은 `Could not connect to Blender`를 반환했다. 이는 도구 등록 실패가 아니라 Blender 앱 또는 애드온 서버가 실행 중이지 않은 현재 연결 상태다. 실시간 Blender 장면과 뷰포트 캡처는 아래 시작 절차 뒤 다시 확인한다.

### 상위 Visual 조립·검증 스킬

- 스킬 본문: `C:\Users\CKIRUser\RPGpro\.agents\skills\rpgpro-visual-slice\SKILL.md`
- UI 메타데이터: 같은 폴더의 `agents\openai.yaml`
- 호출 이름: `$rpgpro-visual-slice`
- 역할: 승인된 Visual Slice에서 최신 기준 이미지와 고정 프레임을 먼저 대조하고, 큰 실루엣→플레이 가독성→색→재질·detail density→조명·효과 순서로 실제 Godot 화면을 비교한다.
- 제작 선택: 기존 자산과 Godot 제작을 우선하고, 이미지 생성은 컨셉 후보, Blender MCP는 고유 형상이 필요한 승인 자산에 한정한다.
- 경계: 정의되지 않은 M05 설계, 미정 아트 규격, 외부 생성 서비스 채택과 사람의 시각·재미 판정을 자동 확정하지 않는다.

새 Codex 작업에서 스킬이 보이지 않으면 프로젝트 루트 `C:\Users\CKIRUser\RPGpro`를 작업 폴더로 열었는지 확인한 뒤 앱을 새로고침하거나 다시 시작한다. 스킬을 홈 디렉터리에 중복 설치하지 않는다.

### MASTER-03R 프로젝트 정본 Skill

다음 네 폴더는 RPGpro를 clone한 뒤 함께 복구돼야 하는 저장소 정본이다.

- `.agents/skills/rpgpro-visual-slice/`: 실제 화면 조립·비교·통과 경계
- `.agents/skills/rpgpro-lowpoly-modeling/`: 게임용 저폴리 형태·스케일·피벗·충돌·NavMesh·모듈 제작
- `.agents/skills/rpgpro-material-texture/`: 공유 Material Family·Color Block·필요한 최소 Texture와 filtering
- `.agents/skills/rpgpro-2d5d-asset/`: 투명 raster·pivot·균일 scale·Billboard/4·8방향·독립 Collision과 Godot 화면 검증

네 Skill에는 사용자 PC 절대경로, API Key·Token·Secret, Blender 개인 설정과 런타임 캐시를 넣지 않는다. 외부 도구의 설치 경로·고정 버전·MCP 시작 순서는 이 문서가 소유하고, 프로젝트 Skill은 그 설치를 내장하거나 홈 디렉터리에 중복 설치하지 않는다.

구조 검증은 Codex의 `skill-creator/scripts/quick_validate.py`로 각 Skill 폴더를 검사한다. 체크포인트 승인 시 네 Skill의 `SKILL.md`, `agents/openai.yaml`과 필요한 `references/`를 다른 승인 정본과 함께 파일별로 명시하여 stage한다. `.codex/config.toml`, `.godot/`, 개인 Save, 설치 파일, 로그와 임시 렌더는 원격 대상이 아니다.

최신 시각 기준 이미지는 `art/reference/`에 있고 `art/.gdignore`로 Godot 런타임 import에서 제외한다. 현재 3D+2.5D 제작 우선순위와 이미지 역할은 `MASTER_03R_2D5D_Visual_Architecture.md`가 소유한다.

### MASTER-03R 2.5D 구조 복구 확인

clone 뒤 다음 항목이 함께 있어야 한다.

- `scripts/visual_2d5d/`의 base script 5개
- `scenes/visual_2d5d/`의 base scene 3개
- `scenes/reference/master_03r_*_validation.tscn` 2개
- `assets/art/green_coast_2d5d_v01/`의 config, 독립 sprite와 표면 sample
- `tools/master_03r_prepare_sprites.py`, `master_03r_extract_surface_samples.py`, `master_03r_verify.gd`, `master_03r_capture.gd`

Godot import 뒤 `res://tools/master_03r_verify.gd`를 실행하면 필수 resource, 비율, 표면 크기, 3D foundation, 한글 source text, 방향, 이동과 proxy 반응을 검사한다. 실제 화면은 Compatibility/ANGLE 일반 그래픽에서 `master_03r_capture.gd`를 실행해 확인하며 headless dummy renderer만으로 시각 합격을 선언하지 않는다.

### SCENARIO-01 프로젝트 범위 생산 환경

- 공식 MCP URL: `https://mcp.scenario.com/mcp`
- 로컬 등록: `.codex/config.toml`의 `[mcp_servers.scenario]`, Streamable HTTP + OAuth. 이 파일과 OAuth/token은 Git 대상이 아니다.
- 공식 Skill upstream: `scenario-labs/skills`, 설치 기준 commit `168e6682d6cd9f2fd4b6196c1c904aa449048e1a`, 설치일 2026-09-07.
- 프로젝트 설치 Skill: `scenario`, `scenario-image`, `scenario-image-editing`, `scenario-game-assets`, `scenario-consistency`, `scenario-identity-library`, `scenario-model-training`, `scenario-asset-analysis`, `scenario-quality-gate`, `scenario-refine-loop`, `scenario-sprite-animation`.
- 프로젝트 Director: `.agents/skills/rpgpro-scenario-production/`. 공식 API 사용법을 복제하지 않고 RPGpro reference, camera, quality, cost, Godot 반입 순서를 소유한다.
- Registry와 상세 절차: `docs/art/SCENARIO_REFERENCE_REGISTRY.md`, `docs/SCENARIO_01_Production_Pipeline.md`.

`scenario-sprite-animation`의 후속 video lane은 `scenario-video` sibling이 필요하지만 현재 캐릭터·animation 범위 밖이므로 설치하지 않았다. 해당 범위를 명시적으로 열면 공식 upstream에서 필요한 sibling만 추가한다. `scenario-identity-library`는 validator 호환을 위해 frontmatter의 angle bracket 예시만 의미 보존 형태로 정규화했다.

현재 관리형 PC는 `codex mcp` CLI 실행을 `EPERM`으로 차단한다. 우회하거나 관리자 권한을 올리지 않는다. 새 Codex 작업 또는 앱/MCP 재시작으로 프로젝트 설정을 다시 읽고 OAuth를 완료한 뒤 diagnostics, Team, Project, live image model, asset/collection tool을 실제 호출해야 연결 완료다. 설정 존재만으로 성공 처리하지 않는다.

초기화 PC에서는 clone → 프로젝트 Skill 확인 → `.codex/config.toml`에 위 URL을 OAuth 방식으로 재등록 → Codex 재시작 → OAuth 재인증 → diagnostics/Team/Project/model/tool 확인 → Registry asset ID 재확인 순서로 복구한다. API key, token, Scenario secret, Codex 개인 인증, cache는 저장소에서 복구하지 않는다.

### Blender→Godot 파이프라인 확인 자산

- Blender 원본: `C:\Users\CKIRUser\BlenderTrials\B2\b2_rock_01.blend` — 현재 SHA256 `11b944d1581e788519e3855fae303392939015d24b4d3345cef966afd46966fa`.
- GLB 출력: `C:\Users\CKIRUser\BlenderTrials\B3\b2_rock_01.glb`.
- Godot 반입본: `res://assets/prototype/blender/rocks/b2_rock_01.glb`.
- 두 GLB는 크기 `4,440 bytes`, SHA256 `11841ead7734be97b63868a758753c15dc48ea7cccd198c091fe9ae55f8344fc`로 일치한다.
- 독립 확인 장면: `res://scenes/prototype/p2_rock_import_test.tscn`.

이 자산은 Blender→GLB→Godot 반입 경로의 기술 확인물이다. 최종 아트 적합성, 게임 내 크기감, 충돌과 성능을 통과한 정식 에셋으로 간주하지 않는다.

## 복구 및 종료 절차

### 시작

1. 허용된 도구를 준비한다.
2. 새 PC라면 `git clone https://github.com/SHKK120/RPGpro.git` 후 `main` 브랜치를 연다. 기존 작업 폴더가 있으면 무조건 pull·reset하지 않고 미커밋 변경부터 확인한다.
3. 실제 작업 경로를 확인한다.
4. `00_시작.md`와 `40_구현순서.md`를 읽는다.
5. 지정된 조각을 시작한다.

### 시각 작업 환경 복구

1. 위 Godot, Blender, uv/uvx와 MCP용 Python 경로가 실제로 존재하고 각 버전이 맞는지 확인한다.
2. `.codex/config.toml`에서 `rpgpro_blender_b1`의 절대 경로, `127.0.0.1:9876`, safe mode, telemetry 차단, 승인 모드와 관찰 도구 목록을 확인한다. 홈 `~/.codex/config.toml`로 옮기지 않는다.
3. Blender를 위 Portable 실행 파일로 열고 사용할 승인 `.blend`를 연다. 다른 열린 파일의 미저장 작업을 닫거나 덮어쓰지 않는다.
4. Blender 3D View의 N 사이드바에서 MCP for Blender 패널을 열고 애드온 서버를 시작한다. 외부 에셋·생성 서비스 체크박스와 API 키는 켜지 않는다.
5. Codex에서 새 작업을 열거나 MCP를 재시작해 프로젝트 설정을 다시 읽는다. 설치·애드온 복사를 먼저 반복하지 않는다.
6. `get_scene_info`를 호출해 열린 파일과 객체를 확인하고, 필요한 경우 `get_object_info`와 `get_viewport_screenshot`으로 관찰한다.
7. 연결 실패 시 Blender 앱, 애드온 활성화와 `127.0.0.1:9876` 리스닝을 먼저 확인한다. 방화벽 개방, 외부 바인딩, PATH·레지스트리 변경, safe mode 해제나 새 MCP 중복 등록으로 우회하지 않는다.
8. M05 등 시각 조각은 범위 문서와 선행 상태가 승인된 뒤 `$rpgpro-visual-slice`를 사용해 시작한다.

### P0 프로젝트 열기

- Godot 프로젝트 관리자에서 `C:\Users\CKIRUser\RPGpro\project.godot`를 가져와 연다.
- 또는 `C:\godot\godot.exe --editor --path C:\Users\CKIRUser\RPGpro`로 연다.
- 현재 F5 메인 씬은 `project.godot`의 `run/main_scene`을 따른다. P0 비교 원본은 `res://scenes/prototype/p0_bootstrap.tscn`이다.
- 현재 F5 장면은 `res://scenes/prototype/green_coast_m02.tscn`이며, 루트 스크립트가 M02→M03→M04→MACRO-01→MASTER-03 계층을 연결한다. MASTER-03 Production Kit은 `res://tools/master_03_build.gd`로 재생성하고, 전체 자동 회귀는 `res://tools/master_03_verify.gd`로 실행한다.
- 초기화 후 최소 확인은 프로젝트 import 완료 → F5 시작 메뉴 표시 → New Game/Continue 진입 → Home과 Green Coast 화면 로드 순서다. PLAYTEST-01 전체 흐름은 `40_구현순서.md`의 재개 지점에서 이어간다.
- CLI import·headless 실행 결과와 사람의 실제 화면 확인 결과는 구분해 기록한다.

### 종료

1. 결과를 확인한다.
2. 승인된 범위에서만 commit·push한다.
3. 원격 반영을 확인한다.
4. 원격에 보존되지 않은 항목을 알린다.

## 주의

- 로컬 `git log`만으로 GitHub 업로드 성공을 선언하지 않는다. 별도 승인으로 push한 뒤 원격 브랜치 커밋을 확인하거나 GitHub에서 확인한다.
- 게임 플레이 세이브는 프로젝트 소스와 별개다. Godot의 `user://` 데이터는 일반적으로 프로젝트 폴더 밖에 있으며, 실제 경로와 개인 데이터 백업은 세이브 조각에서 정한다.
- GitHub에 코드를 올렸다고 플레이 세이브가 자동 보존되는 것으로 취급하지 않는다.
- 인증 토큰, API 키, 계정 캐시, Codex 인증 파일은 저장소, 장부, 스크린샷에 넣지 않는다.
- `.agents/skills/rpgpro-visual-slice/`, `.agents/skills/rpgpro-lowpoly-modeling/`, `.agents/skills/rpgpro-material-texture/`, `.agents/skills/rpgpro-2d5d-asset/`는 프로젝트 재현 규칙이므로 승인된 정본 체크포인트에 포함한다. `.codex/config.toml`은 로컬 경로와 MCP 설정을 담은 PC별 파일이므로 Git에서 제외하고 위 절차에 따라 재설정한다.
- Git 체크포인트는 게임 코드와 프로젝트 문서를 보존하지만 Godot `user://` 개인 Save, 외부 Blender 원본, 도구 설치 파일과 계정·인증 설정은 보존하지 않는다.
