# Codex 지시서 — 프로젝트 초기 문서 구조 구축

## 이번 작업의 목적

새 게임 프로젝트의 **개발 문서 구조만 초기화**한다.

이번 작업에서는 게임 플레이 코드를 작성하지 않는다.

- `.gd` 파일을 새로 만들지 않는다.
- `.tscn` 파일을 새로 만들지 않는다.
- 게임 디자인의 빈칸을 임의로 결정하지 않는다.
- 새로운 기능을 제안해서 구현하지 않는다.
- commit / push는 아직 하지 않는다.

이 프로젝트에서는 **Codex가 저장소를 직접 수정하는 유일한 구현 AI**다.
ChatGPT는 외부 기획·검토·Codex 지시서 작성 역할이다.
사용자의 현재 지시가 과거 문서보다 우선한다.

---

## 1. 먼저 확인할 것

현재 작업 디렉터리와 저장소 상태를 확인한다.

확인 항목:

- 현재 작업 디렉터리의 절대 경로
- Git 저장소 여부
- `project.godot` 존재 여부
- 현재 최상위 파일/폴더 목록

현재 폴더가 프로젝트 루트가 아니라고 판단되면 파일을 수정하지 말고 그 사실만 보고한다.

---

## 2. 만들 문서 구조

다음 파일과 폴더를 만든다.

```text
AGENTS.md

docs/
  00_시작.md
  01_환경복구.md

  10_정체성.md
  11_규칙_핵심루프.md
  12_규칙_월드탐험.md
  13_규칙_집.md
  14_아트스타일.md
  15_전투.md

  30_데이터구조.md
  31_아이템.md
  32_가구테마.md
  33_지역.md
  34_세이브.md
  35_멀티경계.md

  40_구현순서.md
  50_닫을것.md
  60_나중에.md
  지시장부.md
```

---

## 3. AGENTS.md 규칙

`AGENTS.md`는 길게 쓰지 않는다.

역할은 다음 두 가지뿐이다.

1. 이 프로젝트의 핵심 작업 규율을 짧게 알린다.
2. 작업 전에 반드시 `docs/00_시작.md`를 읽도록 보낸다.

다음 원칙을 포함한다.

- 사용자의 현재 지시가 문서보다 우선한다.
- 사용자가 정하지 않은 게임 디자인을 임의로 확정하지 않는다.
- 작업 전에 `docs/00_시작.md`를 읽는다.
- `docs/00_시작.md`의 라우터가 지정한 문서만 추가로 읽는다.
- 한 작업은 하나의 조각(Slice)으로 제한한다.
- 현재 조각 완료 전에 다음 기능을 구현하지 않는다.
- 미정 결정은 `docs/50_닫을것.md`에 둔다.
- 후순위 아이디어는 `docs/60_나중에.md`에 둔다.
- 게임 코드 수정 후에는 가능한 범위에서 Godot의 기계적 오류를 확인한다.
- 재미의 최종 판정은 사람이 한다.

---

## 4. docs/00_시작.md

이 파일은 매 작업의 **문서 라우터**다.

다음 내용을 포함한다.

### 환경 요약
- 프로젝트 루트
- 엔진: Godot 4 계열
- 현재 단계: 초기 문서 정리 / 게임 코드 구현 전

### 용어
개발 작업 단위는 Godot의 Scene과 혼동하지 않도록 **조각(Slice)** 이라고 부른다.

### 라우터 표
예시 형태:

| 작업 종류 | 읽을 문서 |
|---|---|
| 프로젝트 시작/복구 | 01_환경복구 |
| 게임 정체성/방향 | 10_정체성 |
| 핵심 플레이 루프 | 11_규칙_핵심루프 |
| 월드/탐험/지역 | 12_규칙_월드탐험, 33_지역 |
| 집/가구/이사 | 13_규칙_집, 32_가구테마 |
| 그래픽/카메라/Vista | 14_아트스타일 |
| 전투 | 15_전투 |
| 데이터 구조 | 30_데이터구조 |
| 아이템 | 31_아이템 |
| 저장 | 34_세이브 |
| 멀티 대비 | 35_멀티경계 |
| 현재 구현 순서 | 40_구현순서 |
| 결정되지 않은 것 | 50_닫을것 |
| 나중 아이디어 | 60_나중에 |

### 정본 원칙
- 문서는 **왜 그렇게 정했는지**를 기록한다.
- 실제 수치와 콘텐츠 값은 나중에 게임 데이터가 정본이 된다.
- 같은 사실을 여러 문서에 중복해서 확정하지 않는다.

---

## 5. 각 문서의 초기 내용

현재 확정된 게임 기획을 임의로 만들어 채우지 않는다.

각 문서에는 우선 다음만 작성한다.

- 문서의 역할
- 이 문서가 소유하는 정보
- 이 문서가 소유하지 않는 정보
- 현재 확정된 내용이 없으면 `미정`이라고 표시

빈칸을 AI가 추측해서 채우지 않는다.

---

## 6. docs/35_멀티경계.md

멀티플레이는 현재 구현하지 않는다.

미래 목표와 구조적 경계만 기록한다.

```text
현재:
- 싱글플레이 우선

향후 목표:
- 2~4인 Steam 친구 초대형 협동
- Host-authoritative
- Dedicated Server 운영 없음

현재 코드 구조에서 지킬 경계:
- World 상태와 Player 상태를 분리한다.
- 월드 객체는 고유 ID를 가질 수 있도록 설계한다.
- Player가 World 상태를 무제한 직접 수정하는 구조를 피한다.
- 저장 구조가 특정 플레이어 하나에만 종속되지 않게 한다.

현재 단계에서는 네트워크 코드를 구현하지 않는다.
```

---

## 7. 자동 검증에 대한 규칙

재미를 자동 테스트로 판정하지 않는다.

다음과 같은 **기계적으로 확인 가능한 사실 검사**는 허용한다.

- Godot 프로젝트 파싱 여부
- Resource 로드 오류
- 잘못된 파일 참조
- ID 중복
- Save / Load 데이터 보존 여부

다음은 사람이 판정한다.

- 전투가 재미있는가
- 탐험하고 싶은가
- Vista가 아름답고 보상처럼 느껴지는가
- 집에 돌아오고 싶은가
- 인테리어가 만족스러운가

---

## 8. 이번 작업에서 하지 않을 것

- 게임 플레이 코드 작성
- 플레이어 이동 구현
- 전투 구현
- 아이템 구현
- 집 구현
- 가구 구현
- 월드 생성
- Steam 연동
- 멀티플레이 코드
- 자유 건축
- 농사, 낚시, 펫, 탈것 등 후순위 콘텐츠 추가
- 새로운 게임 디자인의 임의 확정
- 외부 도구 설치
- commit
- push

---

## 9. 작업 완료 보고 형식

작업이 끝나면 아래만 보고한다.

```text
1. 현재 프로젝트 절대 경로
2. Git 저장소 여부
3. project.godot 존재 여부
4. 생성/수정한 파일 목록
5. 미정 상태로 남겨둔 문서 목록
6. 이번 작업에서 게임 코드를 만들지 않았는지 확인
7. git status
```

보고 후 멈춘다.

commit과 push는 사용자의 다음 지시를 기다린다.
