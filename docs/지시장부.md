# 지시 장부

## 최근 지시 — SCENARIO-01 Scenario MCP + Consistency Production Pipeline

▶ 지시 [요약] MASTER-03R 결과를 폐기하지 않고 공식 Scenario MCP와 필요한 공식 Skill만 프로젝트 범위에 연결해, bare prompt 반복 대신 Canonical Reference·Approved Asset ID·Control·Baseline/Delta·Quality Gate·최대 2회 Refine·Godot 2.5D 반입으로 이어지는 생산 기반을 만든다. 비용 발생 generation과 model training, 캐릭터 제작, stage/commit/push는 하지 않는다.

한 일 공식 `scenario-labs/skills` commit `168e6682…`에서 요청된 11개 Skill을 `.agents/skills/`에 설치했다. `rpgpro-scenario-production` Director, 17개 로컬 reference 역할 Registry, Collection 계획, 실제 Camera 수치, Sprite Contract, Proxy/Control, 13항목 Quality Gate, Refine/Cost 규칙과 복구 문서를 추가했다. `.codex/config.toml`에는 공식 URL을 OAuth 방식으로 등록했다.

확인법 공식 upstream tree와 commit, 설치 폴더, 기존 RPGpro Skill 비덮어쓰기와 문서 경계를 확인한다. 현재 프로세스는 새 MCP 설정을 다시 읽지 않았고 관리형 PC가 `codex mcp` CLI를 `EPERM`으로 차단하므로 OAuth·diagnostics·Team/Project·model/asset tool은 완료 처리하지 않는다. Generation·Training·credit 사용은 0이며 stage/commit/push하지 않는다.

▶ 추가 지시 [요약] 가능한 모든 과금 호출의 dry_run·예상 CU 기록, Asset별 누적 CU 상한, 자동 후보 1장, 기본 Refine 1회, 2회째 조건부, 첫 3종 사람 확인 전 대량 생산 금지, lean tool discovery와 Canonical Baseline/Asset Delta 재사용을 강제한다. 완료된 무과금 단계는 반복하지 않는다.

▶ 추가 지시 [요약] 바닥과 암벽 Texture는 면 크기에 맞춰 늘이지 말고 이어 붙여 확장한다. Validation foundation의 top/side UV를 실제 월드 크기와 고정 tile 크기로 계산하고 repeat를 명시하며 자동 검사 항목에 추가한다.

▶ 후속 승인 [요약] 초기화된 컴퓨터에서 현재 작업을 이어갈 수 있도록 SCENARIO-01까지의 정본 체크포인트를 GitHub에 선별 commit/push한다. `.codex`, OAuth/API key/token, cache, 로그, 임시 Movie Maker 시퀀스는 제외하고 프로젝트 Skill·reference·대표 검토 PNG·Godot 자산·Scene·Script·검증·문서를 명시적으로 포함한다.

## 최근 지시 — MASTER-03R 구조 전환

▶ 지시 [요약] 진행 중인 MASTER-03을 중단·전면 되돌림하지 않고 현재 파일·에셋·문서·Skill·Material·Scene·검증을 먼저 보존한다. Terrain/Collision/NavMesh/Water/Camera/Gameplay/Save/Combat/Input은 재사용하고, 저품질 3D visual은 2.5D 교체 후보로 분류해 **3D Gameplay + 2.5D Visual** 구조를 우선한다. 주인공·몬스터는 디자인하지 않는다.

▶ 추가 지시 [요약] 평평한 도면에 텍스트만 입힌 결과를 금지하고 한글 깨짐, 이미지 늘어남, 원래 구상과의 이탈을 원본 첨부 이미지와 대조한다. 최신 이미지가 ART-01보다 우선한다. Grass·Soil/Path·Rock Cliff·Sand·Shallow Water 표면의 포함 여부를 확인한다. `test.png`는 조립 목표, `test2.png`는 Material/Transition/Module 규격 기준으로만 사용하고 이미지를 통째로 게임에 넣지 않는다.

한 일 기존 MASTER-03을 보존한 채 네 번째 프로젝트 Skill과 3D/2.5D 분리 base를 추가했다. 환경 4종 투명 visual, 기술 방향 marker, 표면 5종 mirror tile, 실제 3D 지면·단차·절벽·길·해안·계단·물 위의 Assembly/Gameplay validation을 만들었다. 첫 캡처에서 과노출, 한 축 UV 늘어남, 사각 그림자와 평평한 slab를 발견해 수정했다. Actor visual의 중복 StaticBody self-collision도 제거하고 visual proxy를 별도 layer로 분리했다. 검토 에이전트 0.

확인법 Godot 4.7.2 headless import와 MASTER-03R 자동 검사 48항목을 통과했다. Compatibility/ANGLE 실제 프레임에서 독립 환경 visual, 3D 단차·절벽·수면, 깨지지 않은 한글과 원본 비율, 월드 크기 기반 Ground/Cliff tile 반복을 확인했다. 최신 목표 수준의 밀도·자연스러운 transition·장시간 시각·재미는 사람 확인 전 통과 처리하지 않는다. stage/commit/push하지 않았다.

## 최근 지시 — MASTER-01 / PHASE B

### PLAYTEST-01 통합 점검·즉시 보정

▶ 지시 [요약] MACRO-01 전체 골격의 New Game부터 Continue까지 실제 흐름을 점검하고 진행 차단·조작 회귀·Home·Region 2·Save의 큰 문제만 즉시 수정하되 새 기능과 Git 보존은 하지 않는다.

▶ 추가 지시 [요약] Shift 달리기를 추가하고, 다음 맵이 끊겨 갈 수 없는 문제를 수정한다.

한 일 코드·씬·상태 흐름을 대조해 검 접촉 프레임 누락 가능성, Landmark/봉인 구역을 관통하는 Region 2 NavMesh, 모순된 확장 Save 상태 허용을 수정했다. Shift를 누르는 동안 WASD·우클릭 이동을 1.65배로 처리하고, 기존 집 충돌체 안에 있던 Region 2 Gate를 집 동쪽 이동 가능 영역으로 옮겨 안내선을 추가했다. 새 Region·보스·아트패스는 추가하지 않았다. 검토 에이전트 0.

확인법 신규 참조, 공격 대상별 1회 제한, 세 구획 NavMesh, 지역 전환 정리와 Save 기본값·정합성을 정적으로 확인했다. Godot 직접 실행은 `Access Denied`, Windows 앱 제어는 활성 사용자 입력 감지로 중단되어 실제 F5 전체 루프와 사람 판정은 통과 처리하지 않는다. stage/commit/push하지 않았다.

### 검 접촉 판정 + MACRO-01 시작

▶ 지시 [요약] 검에 실제로 닿을 때 피해가 적용되도록 판정을 바꾸고 페이즈 2를 진행하라는 지시. 이어서 높은 추론 강도 기준 적용을 요청했다.

한 일 거리·전방 각도로 적 하나를 고르던 공격을 검과 함께 회전하는 Area3D 접촉 판정으로 교체했다. 기존 M02~M05를 재사용해 Home Storage·진행판·테마·지역 통로, Region 2 Prototype의 자원·강한 적·Landmark·자동 Vista·집터·미래 보스 공간·귀환, Home 제작물과 기존 Save 호환 필드를 추가했다. 명칭과 수치는 시험 선택이다.

확인법 신규 참조 존재, 괄호 균형, 메인 씬 연결과 기존 공격 선택 함수 제거를 정적으로 확인했다. Godot 프로세스 실행은 로컬 명령 실행기의 `Access Denied`로 차단되어 파싱·일반 그래픽·전체 루프를 통과 처리하지 않는다. 사람 확인과 stage/commit/push도 수행하지 않았다.

### M05 Visual Baseline 추가 보완

▶ 지시 [요약] 집 지붕 겹침, 고정된 이동 자세, 팔에서 떨어진 검 궤적을 보완하고 마우스를 바라보는 기준으로 삼되, 최종 캐릭터가 2D이므로 360도 연속 회전 대신 4방향 또는 8방향을 사용하라는 지시.

한 일 단일 45도 지붕 판을 두 경사면과 용마루로 교체했다. 플레이어 시각 자식에 양팔·양발 보행 주기와 약한 상하 움직임을 추가하고, 마우스 월드 방향을 8방향으로 고정했다. 공격은 플레이어 오른쪽 어깨 피벗에서 공격 팔·손·검이 함께 회전하며 실제 근접 판정도 같은 양자화 방향을 사용한다. 캐릭터 재질을 Cloth/Outer Cloth/Leather/Skin/Metal 슬롯으로 나눠 이후 2D 픽셀 단면·8방향 텍스처 교체 기준을 남겼다.

확인법 Godot 4.7.2의 Compatibility/ANGLE 일반 그래픽으로 변경 전후 Gameplay·Home·보행·공격 프레임을 직접 캡처했다. 실행 검사에서 기존 관통 지붕 제거, 두 지붕 면, 8방향 인덱스, 양발 보행 차이, 오른쪽 무기 피벗, 공격 중/후 팔·검 표시 복귀와 치명 피해 뒤 장면 유지·HP 100 거점 복귀를 확인했다. 시각 만족과 실제 마우스·키보드 조작감은 사람 확인 대기다. stage/commit/push하지 않아 원격 미보존이다.

▶ 지시 [요약] M05 Visual Baseline과 MACRO-01을 한 흐름으로 관리하되 첫 실행에서는 PHASE A만 구현하고, 대표 세 프레임의 사람 확인 전 PHASE B를 시작하지 않는다. 현재 M04 구현은 사용 가능하지만 사람 확인 완료로 기록하지 않고 필요한 기능만 기술 회귀 확인한다.

자리 `scenes/prototype/green_coast_m02.tscn`, `scripts/prototype/m02_green_coast.gd`, `m03_green_coast.gd`, `m03_player.gd`, `m04_green_coast.gd`와 Green Coast 재질·셰이더에 시각 기준선을 적용했다. 기준과 재개 조건은 `docs/MASTER_01_Visual_to_Game_Skeleton.md`, 현재 상태는 `docs/40_구현순서.md`가 소유한다.

한 일 기존 에셋을 우선 재사용해 플레이어·근접·원거리·정예·보스 실루엣, 공유 재질군, 해안 팔레트, 저비용 바다, Home 조명과 Vista 원경 깊이를 정리했다. 외부 에셋·새 플러그인·외부 AI·Blender 재작업은 하지 않았고 PHASE B도 시작하지 않았다. 검토 에이전트 0.

확인법 기술 확인과 사람 판정을 분리한다. 마지막 HUD/표식 미세 조정 직전의 장면 파싱·Compatibility 일반 실행·대표 프레임은 확인했다. 조정본 재실행과 M04 사망 복구·외곽 보스 입구 최종 회귀는 로컬 명령 실행기의 `Access Denied`가 해소된 뒤 확인한다. 사람이 볼 것은 Gameplay의 게임다움, Home/Vista의 분위기·깊이, 이 기준선으로 전체 게임을 확장해도 충분한지의 세 묶음이다. 현재 상태는 최종기술확인차단이며 stage/commit/push하지 않아 원격 미보존이다.

### 028 — MASTER-01 PHASE B / MACRO-01 시작

▶ 지시
[요약] 검 접촉 피해 판정으로 변경하고 PHASE B를 진행한다.

자리
`scripts/prototype/m03_green_coast.gd`, `scripts/prototype/macro_01_game.gd`, `scenes/prototype/green_coast_m02.tscn`과 `docs/MACRO_01_First_Game_Skeleton.md`.

한 일
실제 칼날 Area 접촉 판정과 Home→Green Coast 완료→Region 2→Home→제작→Save/Continue 골격을 구현했다. 기존 시각 기준과 M02~M04 상태를 재사용하고 최종 디자인은 확정하지 않았다. 검토 에이전트 0.

확인법
정적 구조 확인 완료. Godot 파싱·일반 실행·상태 흐름과 사람 확인은 대기하며 stage/commit/push하지 않는다.

## 문서의 역할

사용자 지시로 바뀐 프로젝트 방향과 작업 규율뿐 아니라, 그 지시로 실제 수행한 구현·문서 작업 이력을 관리한다.

## 이 문서가 소유하는 정보

- 사용자 지시의 요약
- 지시가 영향을 주는 문서와 조각
- 기존 결정의 변경 또는 폐기 이력
- 실제 수행한 작업과 확인 결과

## 이 문서가 소유하지 않는 정보

- 게임 디자인의 정본
- 구현 세부 사항
- 추측으로 만든 결정

## 현재 확정된 내용

### 001 — DOC-02 / A

▶ 지시
[요약] `instruction_02_reviewed.md`를 읽고 A만 수행하며 B~E와 게임 코드는 진행하지 말라는 사용자 지시.

자리
`AGENTS.md`, `docs/00_시작.md`, `docs/01_환경복구.md`, `docs/40_구현순서.md`, `docs/지시장부.md`, `docs/기록함/README.md`, `docs/기록함/지시서/instruction_02_reviewed.md`의 기록·복구·재개 절.

한 일
A-1~A-5의 운영 문서를 보완하고 기록함 안내와 지시서 보관본을 만들었다. 검토 에이전트 0.

바뀐 정본
작업 규율은 `AGENTS.md`, 문서 라우팅과 소유 경계는 `00_시작.md`, 확인된 환경과 복구 절차는 `01_환경복구.md`, 현재 조각과 다음 행동은 `40_구현순서.md`, 실제 지시·변경 이력은 이 장부가 소유한다.

반경
변경 파일 7개. A의 운영 문서와 기록 보관 경로만 바꾸며 B~E 주제 문서와 게임 코드에는 영향 없음.

확인법
직접 확인: 작업 경로, Git 저장소·브랜치·remote 이름, `project.godot` 부재, Godot 실행 파일과 버전, 대상 파일 내용. 사람이 볼 것: A의 문서 표현과 다음 단계 승인. 미확인: 렌더러, Codex 정확한 제품명·빌드·선택 방법, 한 세션 가용 시간, 이번 변경의 원격 보존.

### 002 — DOC-02 / B

▶ 지시
[요약] A 보고 확인 후 `instruction_02_reviewed.md`의 B만 수행하고 보고하라는 사용자 지시.

자리
`docs/10_정체성.md`, `docs/11_규칙_핵심루프.md`, `docs/12_규칙_월드탐험.md`, `docs/13_규칙_집.md`에 B의 정체성·루프·월드·집 내용을 이관하고, `docs/40_구현순서.md`와 이 장부의 재개 상태를 갱신했다.

한 일
요구·채택 방향, 제안, 미정을 구분하여 B의 네 주제 문서를 보완했다. C~E 및 게임 코드는 수행하지 않았다. 검토 에이전트 0.

바뀐 정본
게임 정체성은 `10_정체성.md`, 핵심 루프는 `11_규칙_핵심루프.md`, 월드 탐험 방향은 `12_규칙_월드탐험.md`, 집과 이사 규칙은 `13_규칙_집.md`가 소유한다. 현재 단계와 다음 행동은 `40_구현순서.md`가 소유한다.

반경
변경 파일 6개. B의 주제 문서 4개와 운영 상태 2개에 한정되며 A 문서, C~E 문서, 게임 코드에는 영향 없음.

확인법
직접 확인: B 대상 파일의 기존 내용, 상태 구분, 대상 파일 존재, UTF-8 내용과 Git 상태. 사람이 볼 것: 요구·채택 방향의 표현, 제안의 채택 여부, 사람 판정 질문과 B 완료 승인. 미확인: 모든 [제안]과 [미정]의 최종 결정, 실제 플레이 결과, 이번 변경의 원격 보존.

### 003 — DOC-02 / C

▶ 지시
[요약] `instruction_02_reviewed.md`의 C만 수행하고 보고하라는 사용자 지시. 함께 전달된 `backup_AB.md`는 참고 자료로만 확인했다.

자리
`docs/14_아트스타일.md`, `docs/15_전투.md`, `docs/31_아이템.md`, `docs/32_가구테마.md`에 C의 시각·전투·아이템·가구 테마 내용을 이관하고, `docs/40_구현순서.md`와 이 장부의 재개 상태를 갱신했다.

한 일
요구·채택 방향, 제안, 예시, 미정을 구분하여 C의 네 주제 문서를 보완했다. D~E, 게임 코드, 에셋 제작과 외부 도구 연결은 수행하지 않았다. 검토 에이전트 0.

바뀐 정본
시각·카메라·에셋 원칙은 `14_아트스타일.md`, 전투 방향은 `15_전투.md`, 아이템 분류와 후보는 `31_아이템.md`, 가구 테마 방향과 예시는 `32_가구테마.md`가 소유한다. 현재 단계와 다음 행동은 `40_구현순서.md`가 소유한다.

반경
변경 파일 6개. C의 주제 문서 4개와 운영 상태 2개에 한정되며 A~B 문서, D~E 문서, 게임 코드에는 영향 없음. `backup_AB.md`는 수정하거나 저장소에 복사하지 않았다.

확인법
직접 확인: C 대상 파일의 기존 내용, 상태 구분, 대상 파일 존재, UTF-8 내용과 Git 상태. 사람이 볼 것: 시각 방향, 전투 성장 방향, 아이템·테마 제안과 C 완료 승인. 미확인: 실제 시각·플레이 결과, 모든 [제안]과 [미정]의 최종 결정, 에셋 파이프라인의 실제 적용, 이번 변경의 원격 보존.

### 004 — DOC-02 / A~C 문서 중간 보존

▶ 지시
[요약] `approve_AC.md`의 조건에 따라 A~C 문서 백업을 승인한다는 사용자 지시.

자리
`docs/40_구현순서.md`와 이 장부에 B·C 외부 본문 검토 결과와 보존 작업을 기록하고, 승인된 문서 21개만 중간 보존 대상으로 삼았다.

한 일
전달받은 검토 결과에 따라 B와 C를 `외부 본문 검토 적합 — 문서 이관`으로 기록했다. 제안·미정 및 게임 기능의 상태는 바꾸지 않았다. 승인된 21개 문서의 checkpoint 보존 절차를 시작했으며 실제 commit/push 결과와 커밋 ID는 최종 보고에서 확인한다. 검토 에이전트 0.

바뀐 정본
B·C의 문서 이관 검토 상태와 다음 행동은 `40_구현순서.md`가 소유한다. 게임 기획의 제안·미정 정본은 변경하지 않았다.

반경
내용 변경 파일 2개, 보존 승인 범위 21개. `instruction.md`, `docs/기록함/모델 설정.md`, Downloads의 임시 지시서, 게임 파일은 제외한다.

확인법
직접 확인: 저장소 경로, `main` 브랜치, `origin`의 안전한 목적지, 준비 이후 승인 파일 내용 불변, staged 변경 부재, 로컬·원격 시작 커밋 일치. 전달된 외부 검토 결과: B·C 문서 이관 적합. 사람이 볼 것: checkpoint와 다음 D 지시. 실제 commit/push 및 원격 커밋 일치는 최종 보고에서 확인한다.

### 005 — DOC-02 / D

▶ 지시
[요약] `docs/기록함/지시서/instruction_02_reviewed.md`를 읽고 D만 수행하며 E와 게임 코드는 진행하지 않고 commit/push 없이 보고하라는 사용자 지시.

자리
`docs/30_데이터구조.md`, `docs/33_지역.md`, `docs/34_세이브.md`, `docs/35_멀티경계.md`에 D의 데이터·지역·세이브·협동 내용을 이관하고, `docs/40_구현순서.md`와 이 장부의 재개 상태를 갱신했다.

한 일
기존 멀티 경계의 확정 범위를 유지하면서 제안과 미정을 구분해 D의 네 주제 문서를 보완했다. E, 게임 코드, 저장·네트워크 구현은 수행하지 않았다. 검토 에이전트 0.

바뀐 정본
데이터 구조 원칙은 `30_데이터구조.md`, 지역 초안은 `33_지역.md`, 저장 방향과 경계는 `34_세이브.md`, 협동의 현재 범위와 향후 검증 항목은 `35_멀티경계.md`가 소유한다. 현재 단계와 다음 행동은 `40_구현순서.md`가 소유한다.

반경
변경 파일 6개. D의 주제 문서 4개와 운영 상태 2개에 한정되며 A~C 문서, E 문서, 게임 코드에는 영향 없음. 기존 미추적 `docs/기록함/모델 설정.md`는 수정하지 않았다.

확인법
직접 확인: D 대상 파일의 기존 내용, A~C 로컬·원격 checkpoint 일치, 상태 구분, UTF-8 내용과 Git 상태. 사람이 볼 것: 데이터·지역·저장·협동의 제안과 D 완료 승인. 미확인: 모든 [제안]과 [미정]의 최종 결정, 실제 Save/Load와 협동 동작, 이번 D 변경의 원격 보존.

### 006 — DOC-02 / E

▶ 지시
[요약] `run_E.md`에 따라 기존 지시서의 E만 수행하고, 전달된 D 외부 검토 결과를 반영하라는 사용자 지시.

자리
`docs/50_닫을것.md`에 B~D의 미정 질문을 모으고 `docs/60_나중에.md`에 후순위 아이디어를 정리했다. `docs/40_구현순서.md`에는 D 검토 결과, E 상태, 초안 구현 순서와 다음 행동을 기록하고 이 장부를 갱신했다.

한 일
D를 `외부 본문 검토 적합 — 문서 이관`으로 기록했다. E의 질문·후순위 정리와 첫 구현 논의용 초안 순서를 작성했지만 제안·미정·게임 검증 상태는 승격하지 않았다. 검토 에이전트 0.

바뀐 정본
미정 질문과 결정 이력은 `50_닫을것.md`, 후순위 아이디어와 여는 조건은 `60_나중에.md`, 현재 단계·초안 순서·다음 행동은 `40_구현순서.md`가 소유한다. D의 주제 정본은 다시 작성하지 않았다.

반경
변경 파일 4개. E 대상 문서와 운영 상태에 한정되며 A~D 주제 문서, 게임 코드, `docs/기록함/모델 설정.md`에는 영향 없음. `run_E.md`는 지시서로만 읽고 수정·추적하지 않았다.

확인법
직접 확인: B~D 문서의 미정과 제안, `01_환경복구.md`의 확인 사실, 시작 시 Git 상태, 대상 파일 내용. 전달된 외부 검토 결과: D 문서 이관 적합. 사람이 볼 것: E 문서 내용, D~E 보존 승인, P0 전 최소 결정. 미확인: 모든 미정의 최종 결정, 실제 게임·Save/Load·협동 동작, D~E 변경의 원격 보존.

### 007 — DOC-02 / E 보완 및 D~E 문서 보존

▶ 지시
[요약] `close_docs.md`를 읽고 E를 보완하며 D~E 문서 백업을 승인한다는 사용자 지시.

자리
`docs/50_닫을것.md`의 질문 목록과 결정 시점을 보완하고, `docs/40_구현순서.md`에 E 외부 검토 결과와 다음 행동을 기록했다. 승인된 D~E 문서 8개만 원격 보존 대상으로 삼았다.

한 일
E 외부 검토 결과를 `외부 본문 검토 반영 — 문서 이관 보완 2건 적용`으로 기록했다. 세이브 버전·누락 데이터·중단 복구 질문 Q-018을 추가하고, Q-003에서 P0의 실행 확인과 P1의 입력·카메라 결정 시점을 구분했다. 제안·미정과 게임 검증 상태는 승격하지 않았고 게임 코드는 진행하지 않았다. 검토 에이전트 0.

바뀐 정본
미정 질문과 결정 이력은 `50_닫을것.md`, 현재 단계와 다음 행동은 `40_구현순서.md`가 소유한다. D의 데이터·지역·세이브·협동 정본과 E의 후순위 목록은 보존하되 이번 보완에서 다시 작성하지 않았다.

반경
내용 변경 파일 3개, 보존 승인 범위 8개. `docs/30_데이터구조.md`, `docs/33_지역.md`, `docs/34_세이브.md`, `docs/35_멀티경계.md`, `docs/40_구현순서.md`, `docs/50_닫을것.md`, `docs/60_나중에.md`, `docs/지시장부.md`만 보존 대상으로 삼는다. `close_docs.md`, 다른 지시서, `docs/기록함/모델 설정.md`, 게임 코드와 에셋은 제외한다.

확인법
직접 확인: 저장소·브랜치·원격, A~C 기준 커밋, D~E 실제 diff, 질문 18개와 후순위 14개, 명시적 staged 파일 8개, 커밋과 원격 `main` 일치. 전달된 외부 검토 결과: E의 질문·후순위 분류와 초안 순서는 방향상 적합하며 보완 2건이 필요하다. 사람이 볼 것: D~E 보존 결과와 이후 P0 최소 결정. 미확인: 모든 제안·미정의 최종 결정, 실제 게임·Save/Load·협동 동작, P0 구현 결과.

### 008 — P0 실행 기반

▶ 지시
[요약] `start_P0.md`를 읽고 P0 시험안으로 구현하되 commit/push하지 말라는 사용자 지시.

자리
저장소 루트에 Godot 프로젝트 설정을 만들고 `scenes/prototype/p0_bootstrap.tscn`에 정적 3D 시험 장면을 배치했다. `docs/41_P0_실행기반.md`에 P0 범위, 여는 방법, 사람 확인 목록과 실제 기술 확인 결과를 기록했다.

한 일
Compatibility, 1280×720 창 모드, 높은 사선 고정 카메라를 P0 임시 설정으로 선택했다. 바닥, 실제 DirectionalLight3D와 WorldEnvironment, 임시 캡슐 하나와 서로 다른 상자 두 개를 엔진 내장 메시·재질로 구성했다. 이동·카메라 조작·충돌·게임 스크립트와 P1 이후 기능은 만들지 않았다. 검토 에이전트 0.

바뀐 정본
P0 시험 설정과 확인 목록은 `41_P0_실행기반.md` 및 `project.godot`, `p0_bootstrap.tscn`이 소유한다. 환경 복구는 `01_환경복구.md`, P0 선택과 남은 P1·최종 미정은 `50_닫을것.md`, 현재 재개 지점은 `40_구현순서.md`가 소유한다.

반경
의도한 변경 파일 9개: `.gitignore`, `project.godot`, `scenes/prototype/p0_bootstrap.tscn`, `docs/00_시작.md`, `docs/01_환경복구.md`, `docs/40_구현순서.md`, `docs/41_P0_실행기반.md`, `docs/50_닫을것.md`, `docs/지시장부.md`. Godot가 만든 `.godot/` 캐시는 `.gitignore`로 제외한다. 기존 미추적 지시서와 `docs/기록함/모델 설정.md`는 건드리지 않았다.

확인법
직접 확인: Godot `4.7.2.stable.official.ed1daf0bf`, headless import, 메인 씬 3회 반복 로드, 일반 그래픽 창 생성·종료 후 같은 경로 재실행, Compatibility 1280×720 프레임 출력과 장면 물체·그림자. 일반 그래픽 출력은 Intel HD Graphics 530에서 ANGLE로 완료됐고 OpenGL 3.3 품질 경고가 있었다. headless Movie Maker 보조 시도는 dummy renderer 오류로 실패했으나 일반 그래픽 출력은 성공했다. 사람이 볼 것: 실제 창의 가독성, F5/F8, 에디터를 닫았다 다시 연 뒤 재실행. 미확인: 사람의 P0 수용, 최종 렌더러·아트·캐릭터 표현, P1 입력. 이번 P0는 commit/push하지 않는다.

### 009 — P0 사람 확인 및 첫 구현 보존

▶ 지시
[요약] `finish_P0.md`를 읽고 P0 사람 확인 완료를 기록하며 지정된 P0 파일의 백업을 승인한다는 사용자 지시.

자리
`docs/41_P0_실행기반.md`에 사용자가 직접 확인한 실행 화면, 종료, 재열기 결과를 기록하고 `docs/40_구현순서.md`의 P0 상태와 다음 행동을 갱신했다. P0 실제 파일 9개만 원격 보존 대상으로 삼았다.

한 일
사용자 보고에 따라 바닥·캡슐 하나·상자 두 개의 표시와 형태·깊이, 입력 없는 고정 구도, F8 종료, 에디터 재열기 후 같은 화면 재실행, 새 실행 차단 오류 없음에 대해 P0 실행 기반 사람 확인을 완료했다. 성능·최종 아트·게임 재미, P1 입력과 최종 렌더러는 검증 또는 확정하지 않았다. 검토 에이전트 0.

바뀐 정본
P0 사람 확인 결과는 `41_P0_실행기반.md`, 현재 상태와 P1-1 전 중단 지점은 `40_구현순서.md`가 소유한다. 프로젝트와 씬의 P0 임시 설정은 `project.godot`과 `scenes/prototype/p0_bootstrap.tscn`이 소유한다.

반경
내용 변경 파일 3개, 보존 승인 범위 9개: `project.godot`, `scenes/prototype/p0_bootstrap.tscn`, `.gitignore`, `docs/41_P0_실행기반.md`, `docs/00_시작.md`, `docs/01_환경복구.md`, `docs/40_구현순서.md`, `docs/50_닫을것.md`, `docs/지시장부.md`. `.godot/` 캐시, `instruction.md` 삭제, `finish_P0.md`, 기존 미추적 지시서와 `docs/기록함/모델 설정.md`는 제외한다.

확인법
사용자 확인: 실제 F5 실행 화면, 물체의 형태·깊이와 고정 구도, F8 종료, 에디터 재열기 후 재실행, 새 실행 차단 오류 없음. 기존 기술 확인: import·씬 로드·일반 그래픽 렌더 성공. 기존 headless Movie Maker 실패는 해결됐다고 바꾸지 않는다. 사람이 아직 판단할 것: 성능·최종 아트·재미와 이후 P1-1 시험안. 직접 확인할 보존 결과: 명시적 staged 파일 9개, 커밋, `origin/main` 원격 커밋과 로컬 HEAD 일치.

### 010 — P1-1 최소 이동

▶ 지시
[요약] `start_P1_1.md`를 읽고 P1-1 시험안으로 구현하되 commit/push하지 말라는 사용자 지시.

자리
`scenes/prototype/p1_movement.tscn`에 P0와 분리된 이동·충돌 시험장을 만들고 `scripts/prototype/p1_player.gd`에 최소 캡슐 이동을 구현했다. `docs/42_P1_1_최소이동.md`에 시험 범위와 사람 확인 목록을 기록했다.

한 일
물리 키 WASD를 카메라 수평 기준 방향으로 변환해 속도 4.0의 수평 이동을 만들고, 대각선 길이 제한·중력·`move_and_slide()`를 적용했다. 바닥·고정 상자 두 개·보이는 외곽 막이 네 개에 단순 충돌체를 맞췄다. P0 카메라·조명·재질·Compatibility를 유지했으며 추적 카메라, 점프, 공격, 저장과 이후 기능은 추가하지 않았다. 검토 에이전트 0.

바뀐 정본
P1-1 입력 액션은 `project.godot`, 이동 속도와 처리 방식은 `p1_player.gd`, 시험장 수치·충돌·카메라 연결은 `p1_movement.tscn`이 소유한다. 시험 목적과 사람 확인은 `42_P1_1_최소이동.md`, 현재 재개 지점은 `40_구현순서.md`가 소유한다.

반경
의도한 변경 파일 10개: `project.godot`, `scenes/prototype/p1_movement.tscn`, `scripts/prototype/p1_player.gd`, `docs/42_P1_1_최소이동.md`, `docs/00_시작.md`, `docs/01_환경복구.md`, `docs/40_구현순서.md`, `docs/41_P0_실행기반.md`, `docs/50_닫을것.md`, `docs/지시장부.md`. Godot가 `scripts/prototype/p1_player.gd.uid`를 소스 보조 파일로 생성했다. P0 원본, `.gitignore`, 기획·데이터 문서, 기존 삭제·미추적 파일은 수정하지 않는다.

확인법
직접 확인: Godot import와 스크립트 파싱, P1-1 메인 씬 5회 반복 로드, 네 물리 키 입력 액션·카메라 참조·충돌 노드, P0 원본 diff 없음, 일반 그래픽 Compatibility 실행. 기존 드라이버 품질 경고와 ANGLE 전환은 있었으나 새 스크립트·씬 오류는 없었다. 사람이 볼 것: 실제 WASD 방향·대각선·정지 감각, 상자·외곽 충돌과 빠져나오기, 고정 카메라, F8/F5 재실행, P0 F6 비교. 미확인: 사람의 P1-1 수용, 최종 캐릭터·카메라·입력·성능·아트·재미. 이번 P1-1은 stage/commit/push하지 않는다.

### 011 — P1-1 사람 확인 및 최소 이동 보존

▶ 지시
[요약] `finish_P1_1.md`를 읽고 P1-1 사람 확인을 반영하며 현재 P1-1 변경의 백업을 승인한다는 사용자 지시.

자리
`docs/42_P1_1_최소이동.md`에 사용자가 확인한 조작·충돌·재실행 결과를 기록하고 `docs/40_구현순서.md`의 P1-1 상태와 다음 행동을 갱신했다. P1-1 구현·운영 후보 11개만 원격 보존 대상으로 삼았다.

한 일
사용자 보고에 따라 WASD 이동, 키 해제 시 수평 정지, 장애물 안쪽 진행 차단, W+A 입력의 부드러운 벽 슬라이딩, 외곽 모서리 차단, 고정 카메라, 재실행과 P0 비교에 대해 P1-1 최소 이동 사람 확인을 완료했다. 기존 OpenGL 3.3 품질·ANGLE 전환 경고는 해결된 것으로 바꾸지 않았다. 게임 동작·물리·렌더러는 수정하지 않았고 검토 에이전트 0.

바뀐 정본
P1-1 사용자 확인은 `42_P1_1_최소이동.md`, 현재 통과 상태와 P1-2 전 중단 지점은 `40_구현순서.md`가 소유한다. 입력·이동·충돌의 실제 구현 정본은 기존 `project.godot`, `p1_player.gd`, `p1_movement.tscn`을 변경 없이 보존한다.

반경
내용 수정 파일 3개. 보존 승인 범위 11개: `project.godot`, `scenes/prototype/p1_movement.tscn`, `scripts/prototype/p1_player.gd`, `scripts/prototype/p1_player.gd.uid`, `docs/42_P1_1_최소이동.md`, `docs/00_시작.md`, `docs/01_환경복구.md`, `docs/40_구현순서.md`, `docs/41_P0_실행기반.md`, `docs/50_닫을것.md`, `docs/지시장부.md`. P0 씬, `.godot/`, `instruction.md` 삭제, 모델 설정, 승인 밖 지시서와 Downloads의 `finish_P1_1.md`는 제외한다.

확인법
사용자 확인: 실제 WASD·정지·충돌·벽 슬라이딩·외곽 모서리·고정 카메라·재실행·P0 비교. 기존 기술 보고: import·GDScript 파싱·메인 씬 로드·일반 Compatibility 실행과 구성 확인. 미확인: 대각 속도의 사람 수치 측정, 최종 캐릭터·입력·카메라·아트·렌더러, 성능·재미, 우클릭·경로 탐색. 직접 확인할 보존 결과: 명시적 staged 파일과 개수, 커밋, `origin/main`의 실제 원격 커밋과 로컬 HEAD 일치.

### 012 — P1-2 우클릭 목적지 이동

▶ 지시
[요약] `start_P1_2.md`를 읽고 P1-2 시험안으로 구현하되 commit/push하지 말라는 사용자 지시.

자리
기존 `p1_movement.tscn`과 `p1_player.gd`에 정적 내비게이션, 우클릭 바닥 선택, 목적지 상태와 WASD 우선 취소를 최소 추가했다. `p1_navigation_mesh.tres`와 재현 가능한 `p1_bake_navigation.gd`, `docs/43_P1_2_우클릭이동.md`를 만들었다.

한 일
첫 레이 충돌체가 명시적 이동 바닥의 윗면일 때만 클릭을 검사하고, 가까운 NavMesh 점과 도달 경로 끝점을 확인한 뒤 새 목적지를 수락하도록 했다. 새 유효 클릭은 목표를 교체하고, 무효 클릭은 기존 명령을 유지하며, WASD 키 입력은 대기 클릭·자동 이동·표식을 즉시 취소한다. 자동 이동은 P1-1 속도·중력·`move_and_slide()`를 공유하고 도착 또는 무진행 시 멈춘다. 검토 에이전트 0.

바뀐 정본
우클릭 액션은 `project.godot`, 입력 우선순위와 이동 상태는 `p1_player.gd`, Agent·Region·표식 연결은 `p1_movement.tscn`, 베이크 데이터는 `p1_navigation_mesh.tres`가 소유한다. 시험 설명과 사람 확인은 `43_P1_2_우클릭이동.md`, 현재 재개 지점은 `40_구현순서.md`가 소유한다.

반경
구현 변경은 `project.godot`, `scenes/prototype/p1_movement.tscn`, `scripts/prototype/p1_player.gd`, 새 `scenes/prototype/p1_navigation_mesh.tres`와 `tools/p1_bake_navigation.gd`에 한정했다. Godot가 `tools/p1_bake_navigation.gd.uid`를 소스 보조 파일로 생성했다. 문서는 새 `docs/43_P1_2_우클릭이동.md` 및 `docs/00_시작.md`, `docs/40_구현순서.md`, `docs/42_P1_1_최소이동.md`, `docs/50_닫을것.md`, `docs/지시장부.md`만 바꿨다. P0 원본, 환경·기획·데이터 문서, 모델 설정은 수정하지 않았다.

확인법
직접 확인: 경고 없이 정점 23개·폴리곤 25개 NavMesh 베이크, 6개 경로점의 고정 상자 우회 질의, import·스크립트 파싱·메인 씬 12회 반복 로드·일반 그래픽 실행, NavMesh 참조와 입력 우선순위 코드, P0 원본 diff 없음과 WASD 처리 유지. 초기 셀 불일치 경고와 동기화 전 빈 경로는 설정 및 iteration 대기로 해결했으며 기존 ANGLE 환경 경고는 유지됐다. 사람이 볼 것: 실제 클릭 좌표, 표식, 직선·우회 이동, 목표 교체, WASD 취소, 무효 클릭, 도착·근접 목표와 재실행. 미확인: 사람의 P1-2 수용, 우클릭과 WASD의 최종 병행, 성능·최종 아트·캐릭터·카메라·전투 입력. 이번 P1-2는 stage/commit/push하지 않는다.

### 013 — P1-2 / FIX-01 우클릭 자동 이동 수정

▶ 지시
[요약] `fix_P1_2.md`를 읽고 표식은 나오지만 캡슐이 움직이지 않는 우클릭 이동 오류만 수정하되 commit/push하지 말라는 사용자 지시.

자리
최초 사용자 실패를 `P1-2 — 사람 확인 실패·수정 중 (표식만 표시, 자동 이동 없음)`으로 기록하고, 실제 `p1_movement.tscn`의 Player·NavigationAgent3D 상태와 물리 프레임 위치 변화를 단발 진단했다.

한 일
목적지 수락과 11개 경로점 생성 뒤에도 Agent 경로 인덱스가 0에 머물고 수평 속도가 0인 상태로 1.5초 무진행 종료되는 것을 재현했다. 캡슐 제어 원점 Y=1.4와 NavMesh 경로면 Y=0.5의 높이 차이 0.9가 Agent의 경로점 도달 거리 0.35보다 커 첫 점을 넘지 못한 것이 원인이었다. Agent의 `path_height_offset`을 -0.9로 보정했으며 다른 이동 규칙은 바꾸지 않았다. 검토 에이전트 0.

바뀐 정본
FIX-01의 실제 구현 변경은 `scenes/prototype/p1_movement.tscn`의 Agent 설정 한 줄이 소유한다. 최초 실패·원인·진단 결과는 `43_P1_2_우클릭이동.md`, 현재 재개 상태는 `40_구현순서.md`가 소유한다. 기존 미커밋 P1-2 구현과 지시서 보관 커밋은 되돌리지 않았다.

반경
이번 오류 수정 파일은 `scenes/prototype/p1_movement.tscn`, 문서 기록은 `docs/43_P1_2_우클릭이동.md`, `docs/40_구현순서.md`, `docs/지시장부.md`에 한정했다. 진단에 사용한 임시 `tools/p1_click_diagnostic.gd`는 확인 후 제거했다. `p1_player.gd`, `project.godot`, NavMesh·베이크 도구, P0 원본과 다른 게임 기능은 이번 FIX에서 수정하지 않았다.

확인법
실제 P1 씬과 실제 Player를 인스턴스화하고 화면 클릭만 기존 목적지 수락 함수 호출로 대체했다. 열린 바닥은 100 물리 프레임에 6.67 이동해 목표 수평 거리 0.333에서 정지했고, 왼쪽 고정 상자 뒤는 6개 경로점을 따라 159 프레임에 수평 거리 0.346에서 정지했다. 자동 이동 중 `move_right` 입력 주입으로 즉시 취소·표식 제거됐고 키 해제 후 20프레임 동안 재출발하지 않았다. 화면의 실제 우클릭 좌표·체감과 목적지 교체·무효 클릭은 사람 재확인 대기다. 상태는 `P1-2 — 수정완료·사람재확인대기`. stage/commit/push하지 않는다.

### 014 — P1-2 사람 재확인 및 구현 보존

▶ 지시
[요약] `finish_P1_2.md`를 읽고 수정 후 확인 결과를 반영하며 P1-2 구현·수정과 조건을 충족하는 기존 지시서 보관 커밋의 백업을 승인한다는 사용자 지시.

자리
`docs/43_P1_2_우클릭이동.md`에 최초 실패, FIX-01 기술 진단과 수정, 수정 후 사용자 재확인을 순서대로 연결하고 `docs/40_구현순서.md`의 현재 상태와 다음 행동을 갱신했다. P1-2 보존 후보 12개의 실제 존재·변경과 기존 미푸시 커밋 범위를 확인했다.

한 일
사용자의 “확인 완료. 이제 경로 탐색해서 잘 움직임” 보고를 실제 우클릭 핵심 경로 이동 재확인으로 반영했다. 목적지 교체·무효 클릭·근접 목표·재실행 등 사용자가 명시하지 않은 세부 사례는 전체 통과로 바꾸지 않았다. 현재 `path_height_offset=-0.9`는 시험 캡슐 원점과 NavMesh 경로면 관계에 따른 설정임을 명시했으며 게임 동작은 추가 수정하지 않았다. 검토 에이전트 0.

바뀐 정본
사용자 확인과 남은 확인 항목은 `43_P1_2_우클릭이동.md`, 현재 상태와 다음 행동은 `40_구현순서.md`, 실제 지시·보존 범위는 이 장부가 소유한다. 내용 수정은 이 문서 세 개뿐이며 구현·설정·리소스는 기존 P1-2 변경을 그대로 보존한다.

반경
새 checkpoint 후보 12개는 `project.godot`, `scenes/prototype/p1_movement.tscn`, `scripts/prototype/p1_player.gd`, `scenes/prototype/p1_navigation_mesh.tres`, `tools/p1_bake_navigation.gd`, `tools/p1_bake_navigation.gd.uid`, `docs/43_P1_2_우클릭이동.md`, `docs/00_시작.md`, `docs/40_구현순서.md`, `docs/42_P1_1_최소이동.md`, `docs/50_닫을것.md`, `docs/지시장부.md`다. 기존 미푸시 지시서 보관 커밋 1개는 별도 이력으로 함께 전송한다. `finish_P1_2.md`, 모델 설정, 승인 밖 새 지시서와 임시 파일은 제외한다.

확인법
사용자 확인: 수정 후 실제 우클릭 경로 이동. 기존 사용자 확인: WASD 이동, 목적지 표식 표시와 WASD 시 표식 제거. 기존 기술 진단: 실제 P1 씬의 직선·고정 상자 우회·도착 정지와 입력 주입 취소. 미확인: 고정 상자 우회의 사람 확인, 목적지 교체, 무효 클릭, 근접 목표, P1-2 재실행과 최종 성능·아트·재미. 직접 확인: 저장소·브랜치·원격, 후보 12개, P0 원본 무변경, staged 범위, checkpoint 커밋과 실제 원격 `main` 일치 여부. 상태는 `P1-2 — 핵심 경로 이동 사람 재확인 완료`다.

### 015 — P1-2 마감 및 P1-3 기본 추적 카메라

▶ 지시
[요약] `start_P1_3.md`에 따라 P1-2의 남은 다섯 항목 사용자 확인을 반영하고, 기존 이동을 유지하는 P1-3 추적 카메라 시험안을 구현하되 commit/push하지 말라는 사용자 지시.

자리
`docs/43_P1_2_우클릭이동.md`에 고정 상자 우회·목적지 교체·무효 클릭·근접 목표·F8/F5 재실행 확인을 기록해 P1-2를 마감했다. 기존 `p1_movement.tscn`의 Camera에 새 `p1_camera_follow.gd`를 연결하고 `docs/44_P1_3_추적카메라.md`에 시험 범위와 사람 확인 목록을 만들었다.

한 일
기존 높은 사선 원근 구도의 Y·회전·FOV와 시작 XZ 오프셋을 유지하면서 플레이어 XZ를 반응 속도 10.0/초의 지수 감쇠로 따라가게 했다. 움직이는 카메라에서도 클릭 순간의 레이 원점·방향을 물리 프레임까지 보존하도록 `p1_player.gd`의 대기 클릭 데이터만 바꿨다. 카메라 수동 입력, 회전·줌·충돌 회피·위치 제한은 추가하지 않았다. 검토 에이전트 0.

바뀐 정본
추적 방식과 시험값은 `p1_camera_follow.gd`, 카메라·대상 연결은 `p1_movement.tscn`, 클릭 순간 레이 보존은 `p1_player.gd`가 소유한다. P1-3 시험 설명과 사람 확인은 `44_P1_3_추적카메라.md`, 현재 상태는 `40_구현순서.md`, 임시 선택과 최종 미정은 `50_닫을것.md`가 소유한다.

반경
구현은 새 `scripts/prototype/p1_camera_follow.gd`와 엔진 생성 UID, `scenes/prototype/p1_movement.tscn`, 필요한 클릭 레이 부분의 `scripts/prototype/p1_player.gd`에 한정했다. 문서는 새 `docs/44_P1_3_추적카메라.md` 및 `docs/00_시작.md`, `docs/40_구현순서.md`, `docs/43_P1_2_우클릭이동.md`, `docs/50_닫을것.md`, `docs/지시장부.md`만 바꿨다. `project.godot`, P0 원본, NavMesh·베이크 도구와 `path_height_offset`은 변경하지 않았다.

확인법
직접 확인: Godot import·GDScript 파싱·메인 씬 짧은 headless 로드. 실제 P1 씬에서 WASD 수평 2.0 이동 뒤 카메라가 기대 오프셋 위치 오차 0으로 수렴했고 Y·회전·FOV가 유지됐다. 자동 이동 중 카메라는 1.566 이동했으나 표식 월드 위치 변화는 0이고 Agent 높이 보정은 -0.9였다. 단발 진단은 실제 화면 우클릭을 생략했고 임시 파일은 제거했다. 사람이 볼 것: 시작·정지, WASD·우클릭 추적 체감, 추적 중 재지정·취소, 경계·정지 안정과 F8/F5. 상태는 `P1-3 — 구현완료·사람확인대기`; stage/commit/push하지 않는다.

### 016 — P1-3 일부 확인 및 P1-4 목적지 보정

▶ 지시
[요약] `start_P1_4.md`에 따라 P1-3의 부드러운 추적·추적 중 클릭 정확도 사용자 확인을 반영하고, 평면 시험장의 보정 가능한 우클릭을 가까운 도달 가능 목적지로 바꾸는 P1-4 시험안을 구현하되 commit/push하지 말라는 사용자 지시.

자리
`docs/44_P1_3_추적카메라.md`에 실제 사용자 확인 두 가지를 반영했다. `p1_player.gd`의 기존 클릭 순간 레이 보존과 물리 처리 사이에 접점·바닥 평면 의도점 및 실제 경로 끝점 선택을 추가하고 `docs/45_P1_4_목적지보정.md`에 단층 한계와 사람 확인표를 만들었다.

한 일
바닥의 실제 BoxShape3D 윗면에서 Y=0을 계산했다. 첫 충돌이 바닥이면 접점을, 같은 NavigationRegion3D 아래의 고정 상자·막이면 접점 XZ의 바닥 투영을 사용하고, 아무 충돌도 없을 때만 저장된 레이를 해당 평면과 교차시켰다. Agent의 맵·navigation_layers로 조회한 비어 있지 않은 경로의 실제 끝점을 목적지와 표식으로 수락한다. 지원 밖 충돌·비정상 레이는 기존 유효 명령을 유지한다. 검토 에이전트 0.

바뀐 정본
P1-4 목적지 해석·경로 끝점 수락은 `p1_player.gd`, 시험 규칙·단층 한계와 사람 확인은 `45_P1_4_목적지보정.md`가 소유한다. P1-3 사용자 확인은 `44_P1_3_추적카메라.md`, 과거 P1-2 거부 규칙과 현행 참조는 `43_P1_2_우클릭이동.md`, 현재 상태는 `40_구현순서.md`, 임시 입력 선택과 최종 미정은 `50_닫을것.md`가 소유한다.

반경
P1-4 구현 변경은 `scripts/prototype/p1_player.gd` 한 파일이며 씬 연결 변경은 필요하지 않았다. 문서는 새 `docs/45_P1_4_목적지보정.md` 및 `docs/00_시작.md`, `docs/40_구현순서.md`, `docs/43_P1_2_우클릭이동.md`, `docs/44_P1_3_추적카메라.md`, `docs/50_닫을것.md`, `docs/지시장부.md`만 바꿨다. 현재 미커밋 P1-3 파일은 유지했고 `project.godot`, 카메라 스크립트·UID·반응 속도, P1 씬, P0 원본, NavMesh·베이크 도구와 Agent 높이 보정은 이번 P1-4에서 변경하지 않았다.

확인법
직접 확인: Godot import·파싱·메인 씬 짧은 headless 로드. 실제 씬의 화면 레이를 물리 처리 함수에 주입한 단발 진단에서 빈 바닥, 상자 윗면·측면, 외곽 막이와 물리 표면 없는 바닥 평면 교점이 각각 NavMesh의 도달 가능한 끝점으로 보정됐고 실제 플레이어가 이동해 도착 거리 안에서 정지했다. 무효 레이는 기존 목표·표식을 유지했고 WASD는 자동 이동을 취소했다. 이는 실제 OS 마우스 클릭이 아니며 임시 진단 파일은 제거했다. 사람이 볼 것: 실제 화면 보정 감각과 일곱 확인 항목. 상태는 `P1-4 — 구현완료·사람확인대기`; stage/commit/push하지 않는다.

### 017 — P1-3·P1-4 사용자 확인 및 함께 보존

▶ 지시
[요약] `finish_P1_3_4.md`에 따라 실제 사용자 확인 범위를 반영하고, 아직 미커밋인 P1-3 추적 카메라와 P1-4 목적지 보정 구현을 함께 checkpoint로 원격 보존하라는 사용자 지시.

자리
`docs/44_P1_3_추적카메라.md`에 부드러운 추적과 추적 중 실제 클릭 정확도를, `docs/45_P1_4_목적지보정.md`에 사물·벽 클릭 이동 확인을 기록했다. `docs/40_구현순서.md`에는 두 조각의 핵심 확인 상태와 남은 항목, 다음 별도 논의 지점을 정리했다.

한 일
P1-3은 `핵심 추적·클릭 사용자 확인 완료`, P1-4는 `사물·벽 목적지 보정 사용자 확인 완료`로 기록했다. 바닥 밖 배경, 모든 윗면·측면 개별 사례, 입력 전환, 근접 목표·재실행 등 사용자가 명시하지 않은 항목은 전체 통과로 바꾸지 않았다. 이번 보존 단계에서는 게임 코드·씬·수치를 다시 수정하거나 실행 진단을 추가하지 않았다. 검토 에이전트 0.

바뀐 정본
P1-3 사용자 확인과 남은 항목은 `44_P1_3_추적카메라.md`, P1-4 사용자 확인·평면 한계와 남은 항목은 `45_P1_4_목적지보정.md`, 현재 상태와 다음 행동은 `40_구현순서.md`, 실제 지시·보존 범위는 이 장부가 소유한다. 내용 수정은 이 문서 네 개뿐이다.

반경
함께 보존할 후보 11개는 `scripts/prototype/p1_camera_follow.gd`, `scripts/prototype/p1_camera_follow.gd.uid`, `scripts/prototype/p1_player.gd`, `scenes/prototype/p1_movement.tscn`, `docs/00_시작.md`, `docs/40_구현순서.md`, `docs/43_P1_2_우클릭이동.md`, `docs/44_P1_3_추적카메라.md`, `docs/45_P1_4_목적지보정.md`, `docs/50_닫을것.md`, `docs/지시장부.md`다. `project.godot`, P0 원본, NavMesh·베이크 도구, 모델 설정과 미추적 지시서는 제외한다.

확인법
사용자 확인: 카메라의 부드러운 추적, 추적 중 실제 우클릭 위치 정확도, 사물·벽 클릭 이동. 기존 기술 진단: 실제 씬의 카메라 수렴·표식 고정과 레이 주입을 통한 빈 바닥·상자·막이·배경 보정, 무효 레이 보존과 WASD 취소. 미확인: P1-3 시작·재지정·경계·재실행 세부, P1-4 배경 실제 클릭·모든 면·입력 전환·근접 목표·재실행, 최종 카메라·입력·아트·렌더러·성능·재미와 다층 지형. 직접 확인: 저장소·브랜치·원격 기준, 후보 11개와 staged 범위, checkpoint 커밋 및 원격 main 일치 여부.

### 018 — Blender-3 + Godot-1 바위 GLB 반입

▶ 지시
[요약] `start_Blender_3_Godot_1.md`를 읽고 저장된 B2 바위의 Blender GLB 내보내기와 별도 Godot 반입 시험을 한 번에 진행하되 commit/push하지 말라는 사용자 지시.

자리
`C:\Users\CKIRUser\BlenderTrials\B3\b2_rock_01.glb`와 `BLENDER_3_report.md`를 만들고, 프로젝트의 `assets/prototype/blender/rocks/b2_rock_01.glb`, `scenes/prototype/p2_rock_import_test.tscn`, `docs/46_Blender_3_Godot_1_바위반입.md`에 반입 산출물·독립 시험장·확인 기록을 두었다. 현재 재개 상태는 `docs/40_구현순서.md`에 반영했다.

한 일
B2 저장본을 Blender 4.5.13 LTS로 다시 열어 단일 메시를 확인하고 표준 glTF 2.0 GLB로 내보냈다. B2 저장본의 위치·회전이 앞선 보고와 달라져 있었으므로 원본은 저장·수정하지 않고 내보내기 메모리 사본에서만 원점·방향을 복구했다. Godot 4.7.2가 GLB를 가져오고 별도 고정 카메라 시험 장면을 헤드리스와 일반 Compatibility 그래픽으로 로드했다. 검토 에이전트 0.

바뀐 정본
반입 시험의 범위와 확인 결과는 `46_Blender_3_Godot_1_바위반입.md`, 현재 조각과 다음 행동은 `40_구현순서.md`, 실제 수행 이력은 이 장부가 소유한다. Blender B3 보고는 프로젝트 밖 시험 산출물이며 최종 아트 정본이 아니다.

반경
새 프로젝트 파일은 GLB와 그 Godot import 메타, `p2_rock_import_test.tscn`, `46_Blender_3_Godot_1_바위반입.md`다. 기존 문서 수정은 `40_구현순서.md`와 이 장부에 한정했다. `project.godot`, P1 씬·이동·카메라·우클릭 코드, NavMesh, B2 원본은 변경하지 않았다. 임시 내보내기·캡처 스크립트와 캡처 이미지는 확인 후 제거했다.

확인법
직접 확인: Blender 메시 크기 `(1.5, 1.2, 0.9)`, 삼각형 46, 재질 1, non-manifold 0; GLB 장면·노드·메시·primitive·재질 각 1; 출력과 프로젝트 반입본 해시 일치; Godot import·장면 파싱·에디터 재열기·일반 그래픽 렌더 성공; Godot 월드 AABB의 바닥 Y=0 접지; 화면의 바위·바닥·그림자 표시. 기존 OpenGL 품질 경고와 ANGLE 전환은 유지됐다. 사람이 볼 것: 실제 모양·크기감·접지·재질과 재실행. 미확인: 최종 아트, 실제 플레이 크기, 성능, 충돌·상호작용·채집, 정식 월드 배치. stage/commit/push하지 않는다.

### 019 — M01 해안·전망 통합 프리뷰

▶ 지시
[요약] 현재 전달된 M01을 단독 실행 기준으로 사용하고 `next.md`를 추가로 요구·실행하지 않은 채, 완료된 바위 반입을 재사용해 해안 이동·바다 표현·전망 진입과 복귀를 한 번에 완성하라는 사용자 지시. 중간 승인 없이 내부 검증·오류 수정 뒤 보고하며 stage/commit/push하지 않는다. 진행 중 추가 지시로 M01 캐릭터를 축소하고 카메라를 높이되 우측으로 틀지 않고 정면축을 유지했으며, 최종 전망에서는 플레이어 윗부분만 화면 중앙 하단에 살짝 보이게 했다. 새 단순 에셋은 Godot 기본 메시·재질을 우선하며 Blender/MCP·외부 에셋·범용 생성기는 사용하지 않는다. 이어 실제 구현·진행 기록의 순서와 벽·사물 클릭 목적지 간격을 대조하고 원인이 확인되면 같은 M01에서 최소 수정하라는 보완 지시를 반영했다.

자리
`scenes/prototype/coast_preview.tscn`에 M01 실행 장면을 두고, `scripts/prototype/m01_coast_preview.gd`가 전망 진입·복귀를 제어한다. 해안 전용 경로는 `scenes/prototype/m01_navigation_mesh.tres`, 재현 도구는 `tools/m01_bake_navigation.gd`가 소유한다. `project.godot`과 `scripts/prototype/p1_player.gd`에는 M01에 필요한 입력과 기본값 호환 옵션만 추가했다. 목표·결과·사람 확인은 기존 `docs/46_Blender_3_Godot_1_바위반입.md`를 확장해 기록하고 현재 상태는 `docs/40_구현순서.md`에 둔다.

한 일
기존 GLB를 길가 바위 두 개와 원경 실루엣 두 개로 재사용했다. Godot 기본 Box·Plane·Capsule·Cylinder 메시와 재질로 단층 산책로, 절벽, 모래 해안, 목책, 전망 표식을 만들고 고정 PlaneMesh의 시간 기반 셰이더로 바다를 가볍게 움직였다. E 진입 시 기존 자동 이동·대기 클릭·표식을 취소하고 플레이어를 잠근 뒤 단일 M01 제어기가 카메라를 전환한다. E/Esc/WASD 복귀와 전망 중 우클릭 거부를 연결했다. M01 캐릭터와 충돌을 높이 2.0·반지름 0.55로 줄이고 평상 카메라를 `(0, 9.5, 14)`와 X `-34.5°`로 높였다. 전망 진입 시에만 앵커 `(0, 5.5, 1.5)`, 주시점 `(0, -0.5, -25)`, FOV `45°`의 정면 하향 바다 구도로 전환해 플레이어 윗부분만 화면 중앙 하단에 살짝 보이게 했다. 진입 위치의 플레이어 XZ를 따라 프레이밍하며 복귀 시 FOV `75°`와 평상 추적을 되돌린다. 구현 순서는 GLB 반입, 해안 배치, P1 이동·카메라 연결, M01 목적지·전망 연결, 실행·기록으로 대조했다. 클릭 간격의 원인이 M01 몸통·Agent 반지름 `0.55`와 NavMesh 베이크 반지름 `1.0`의 불일치 및 목책 시각·충돌 수평 크기 차이임을 확인해, M01 베이크 반지름을 기본 셀에 맞는 안전값 `0.75`로 줄이고 목책 수평 충돌을 시각 메시와 맞춘 뒤 전용 NavMesh만 재베이크했다. 검토 에이전트 0.

바뀐 정본
M01의 임시 장면 수치와 동작은 해당 씬·스크립트·NavMesh가 소유한다. 기술 결과와 미확인은 `46_Blender_3_Godot_1_바위반입.md`, 재개 지점은 `40_구현순서.md`, 승인된 결과 묶음 규율은 `AGENTS.md`, M01 라우팅은 `00_시작.md`가 소유한다. 이번 선택은 최종 Vista 조작·카메라·아트 규칙을 확정하지 않는다.

반경
새 영구 파일은 `coast_preview.tscn`, `m01_navigation_mesh.tres`, `m01_coast_preview.gd`와 UID, `m01_bake_navigation.gd`와 UID다. 기존 변경은 `project.godot`, `p1_player.gd`, `AGENTS.md`, `docs/00_시작.md`, `docs/40_구현순서.md`, `docs/46_Blender_3_Godot_1_바위반입.md`, 이 장부에 한정했다. 기존 GLB·반입 시험 장면, P0, P1 씬·카메라·NavMesh, Blender 원본과 설정은 변경하지 않았다. 일회성 진단·렌더 캡처 파일은 확인 후 제거했다.

확인법
직접 확인: Godot import·파싱·해안 장면 로드, 조정 후 정점 31·폴리곤 29 NavMesh와 9점 시작–전망 경로, 실제 카메라 레이 우클릭 6.8m 이동과 목표 오차 0.333m, 목책 경계 정지, 바다 클릭 거부, 바위 접지, E 진입과 E/Esc/WASD 복귀, 전망 중 우클릭 차단, 옛 목적지 취소. 대표 물리 레이에서 접점→표식 간격은 수정 전 목책 `1.250m`·바위 `0.978m`, 수정 후 목책 `0.860m`·바위 `0.921m`였고 표식과 실제 경로 끝점은 일치했다. 빈 바닥 목적지 오차 `0.000m`, WASD 취소, 전망 복귀 후 클릭 목표 오차 `0.292m`도 확인했다. 공유 코드 변경 뒤 P1 WASD·우클릭·추적 카메라 회귀를 확인했다. 일반 Compatibility 렌더에서 높은 평상 시점의 산책로·바위·바다·원경과, 우측 회전 없는 FOV `45°`의 살짝 하향 전망에서 플레이어 윗부분만 중앙 하단에 보이는 구도, 복귀 후 FOV·이동 복원이 확인됐다. 기존 ANGLE 전환 경고는 유지됐다. Windows 화면 도구의 Godot 창 소유권 식별 오류로 OS 키·마우스 자동화는 하지 못했으며 실제 일반 렌더, 엔진 입력 및 직접 목적지 진단과 구분한다. 사람이 볼 것: 실제 WASD·우클릭과 경계, E/Esc/WASD 전망 복귀, 평상 시점의 바위·바다·원경, 전망 시점의 최종 구도와 재실행. 상태는 `M01 — 해안·전망 통합 구현완료·사람확인대기`; stage/commit/push하지 않는다.

### 020 — M02 Green Coast 첫 탐험·귀환 루프

▶ 지시
[요약] 첨부한 M02 지시를 단독 기준으로 삼아 현재 RPGpro에 집→숲 채집→유적 기념품→해안 전망→지름길 귀환→작업대·전시→저장/이어하기의 작은 완결 루프를 구현하되 stage/commit/push하지 말라는 사용자 지시. 후속 보완으로 사진 기능은 기념품에 넣지 않고, 전망은 지정 위치나 E 상호작용이 아니라 산 위 북쪽의 실제 통행 불가 절벽 끝에 붙어 있을 때만 아래 바다를 내려다보고 벗어나면 자동 비활성화하게 했다.

자리
`scenes/prototype/green_coast_m02.tscn`을 메인 M02 실행 장면으로 두고 `scripts/prototype/m02_green_coast.gd`가 고정 지역 구성·상호작용·진행·저장·자동 절벽 전망을 제어한다. M02 전용 경로는 `scenes/prototype/m02_navigation_mesh.tres`, 재현 도구는 `tools/m02_bake_navigation.gd`가 소유한다. 범위·기술 확인·사람 확인은 `docs/47_M02_Green_Coast_첫탐험귀환.md`, 현재 재개 상태는 `docs/40_구현순서.md`에 기록했다.

한 일
M01/P1과 반입한 바위를 재사용하고 Godot 기본 메시·재질로 집, 왼쪽 작은 숲, 오른쪽 유적, 북쪽 해안, 중앙 귀환 지름길을 구성했다. E는 가까운 채집물·유적 유물·작업대·전시대에만 사용한다. 청록색 유물은 사진이 아닌 독립 기념품이다. 북쪽 절벽의 못 지나가는 경계에 닿으면 현재 X 위치를 기준으로 전망 카메라가 자동 진입하며 옛 목적지와 표식을 취소한다. 전환 뒤 플레이어 이동을 다시 허용해 끝자락에 붙어 있는 동안 전망을 유지하고, WASD 또는 내륙 우클릭 이동으로 범위를 벗어나면 평상 시점으로 자동 복귀한다. E는 전망 토글이 아니고 Esc는 평소 메뉴를 연다. 고정 ID 중복 방지, 작업대의 원자적 비용 소비, 전시 멱등 처리, 검증 후 교체하는 JSON 저장·이전 정상본·손상 저장 거부·안전 위치 복구를 연결했다. 검토 에이전트 0.

바뀐 정본
M02 시험 장면의 동작과 수치는 새 씬·스크립트·NavMesh가 소유한다. 사람 확인과 기술 결과는 `47_M02_Green_Coast_첫탐험귀환.md`, 재개 지점은 `40_구현순서.md`, 최종 미정과 M02 한정 선택은 `50_닫을것.md`, 실제 수행 이력은 이 장부가 소유한다. 사진 기능, 정식 Vista 규칙, 정식 재료 비용과 세이브 정책은 확정하지 않았다.

반경
새 영구 파일은 `green_coast_m02.tscn`, `m02_navigation_mesh.tres`, `m02_green_coast.gd`, `m02_bake_navigation.gd`, `47_M02_Green_Coast_첫탐험귀환.md`다. `project.godot`의 메인 장면을 M02로 바꾸고 라우터·진행표·미정 목록·이 장부만 최소 갱신했다. 기존 미커밋 바위 반입·M01 변경, P0/P1/M01 비교 씬과 NavMesh, Blender 원본·설정은 보존했다. 외부 에셋·Blender/MCP·새 설치·범용 프레임워크는 사용하지 않았다.

확인법
직접 확인: M02 전용 NavMesh 정점 112·폴리곤 116과 네 구간 연결, 실제 카메라 레이의 빈 바닥·경계·유적 벽 목적지와 정지, 메뉴 입력 차단, 상호작용 거리·시야, 획득·설치·전시 중복 방지, 부족 재료 무소비, 정상 저장·이전본·이어하기 복구와 손상 JSON 거부. 북쪽 절벽의 중앙이 아닌 가장자리에서도 자동 전망 진입, 옛 목적지 취소, 전환 뒤 이동 허용, 끝자락 접촉 중 전망 유지, 내륙 이탈 자동 복귀와 재접촉 진입을 확인했다. 일반 Compatibility 렌더에서 집·숲·유적·해안·움직이는 바다·원경과 하향 전망 구도를 확인했다. 2026-09-06 사용자가 실제 플레이·시각·저장 재실행을 포함한 세 확인 묶음의 완료와 관련 결과 백업을 승인했다. 상태는 `M02 — Green Coast 첫 탐험·귀환 루프 사람확인완료·백업승인`이다.

### 021 — DOC-STYLE-01 AI 전용 입체 픽셀 스타일 가이드

▶ 지시
[요약] M02 구현·사용자 확인·원격 백업이 모두 끝난 뒤에만, 3D 월드와 단순한 형태·픽셀풍 질감 및 절제된 현대 효과를 결합한 AI 전용 스타일 가이드 초안을 작성하라는 사용자 지시. 새 문서는 `docs/16_AI_입체픽셀_스타일가이드.md`이며 `14_아트스타일.md`, `40_구현순서.md`, 이 장부만 최소 수정한다. 게임 코드·씬·에셋 제작과 stage/commit/push는 제외한다.

자리
먼저 사용자 확인을 `47_M02_Green_Coast_첫탐험귀환.md`와 진행표에 반영하고, 실행에 필요한 바위 반입·M01·M02 관련 22개 파일만 `adaeb6b5f7aabc4f0508b9f2a0ec836f154f543d`로 커밋해 `origin/main`과 일치시켰다. `.godot`, Codex 설정, 모델 설정과 과거 지시서는 제외했다. 그 뒤 DOC-STYLE-01의 전제가 충족된 상태에서 지정된 정체성·핵심루프·월드·집·아트·전투·아이템·가구·지역·진행 문서와 M01/M02 기록을 읽었다.

한 일
새 16번 문서에 한 줄 정체성, 스타일 목표, 픽셀풍 3D 표현 철학, 기본·전망 카메라, 형태, 색·명도·지역 팔레트, 재질·텍스처, 조명, 캐릭터 후보, 거점·숲·폐허·해안 적용, 가구·꾸미기, AI 제작 우선순위, 금지 방향, 채택 체크리스트와 미정 목록을 작성했다. Godot 기본 제작→AI 이미지 컨셉 보조→필요할 때만 Blender·AI 3D 승격 순서를 두고, 도구가 아니라 실제 화면 적합성으로 후보를 채택하게 했다. 검토 에이전트 0.

바뀐 정본
`14_아트스타일.md`는 상위 시각 원칙을 유지하고 `16_AI_입체픽셀_스타일가이드.md`를 AI 제작·화면 채택용 세부 실행 초안으로 연결한다. 현재 작업 상태와 다음 검토 행동은 `40_구현순서.md`, 실제 수행 이력은 이 장부가 소유한다. 기존 기획 문서의 `[제안]`과 `[미정]`, M01/M02 임시 수치와 Compatibility 결과는 최종 아트 규격으로 승격하지 않았다.

반경
DOC-STYLE-01에서 생성한 파일은 `docs/16_AI_입체픽셀_스타일가이드.md` 하나다. 수정 파일은 `docs/14_아트스타일.md`, `docs/40_구현순서.md`, `docs/지시장부.md` 세 개다. 게임 코드, 씬, 캐릭터, 이미지, Blender·외부 AI 도구와 다른 기획 문서는 변경하지 않았다.

확인법
직접 확인: 지시된 15개 필수 주제와 기존 문서 관계가 새 문서에 모두 존재하고, `14_아트스타일.md`는 요약·관계만 최소 보완했으며, 최종 캐릭터·카메라·해상도·필터링·렌더러·후처리·물·AI 3D 도구는 미정으로 분리했다. DOC-STYLE-01 문서 변경은 staged 없이 로컬에만 있으며 commit/push하지 않았다. 상태는 `DOC-STYLE-01 — 작성완료·검토대기`다.

### 022 — ART-01 Green Coast 첫 환경 에셋팩

▶ 지시
[요약] 정상 M02의 게임 흐름과 저장을 보존하면서 DOC-STYLE-01 초안을 첫 시험 기준으로 적용해 바위·절벽·나무·관목·목책·폐허 잔해·벤치·전시 받침·목재 및 돌 채집물 10종을 제작·개선하고 실제 Green Coast에 적용하라는 사용자 지시. Godot 우선, 외부 이미지·Blender/MCP·다운로드 없이 한 번에 제작·실행·회귀 검증하고 최초 구현에서는 stage/commit/push하지 않는다.

자리
M02는 `adaeb6b5f7aabc4f0508b9f2a0ec836f154f543d`로 원격 보존돼 있고 DOC-STYLE-01 문서가 실제 존재함을 확인했다. `assets/art/green_coast_v01/`에 텍스처·재질, `scenes/art/green_coast_v01/`에 재사용 시각 씬, `tools/art_01_build.gd`에 이 팩 전용 고정 생성 도구를 두었다. M02의 실행 장면과 기능 스크립트에는 새 시각 자산 참조만 연결했다.

한 일
난수 없는 64×64 석재·목재·잎·흙 PNG 네 장과 공유 재질 여섯 개를 만들었다. Nearest with mipmaps 필터와 밉맵 import 설정을 사용하고, UV가 없는 B2 바위에는 원본을 바꾸지 않는 석재 트라이플래너 override를 적용했다. 신규 시각 씬 아홉 개와 기존 바위를 합쳐 10종을 구성했다. 기존 나무·목책·채집물·벤치·전시대의 상태 루트와 충돌은 유지하고 시각 자식만 교체했으며, 절벽 층리·관목·폐허 잔해는 길과 상호작용 영역 밖에 충돌 없이 배치했다. 첫 렌더의 석재 무늬가 촘촘해 반복 밀도를 줄이고 지면 무늬도 낮은 대비의 큰 덩어리로 보완했다. 검토 에이전트 0.

바뀐 정본
실제 형태·텍스처·재질 수치는 ART-01 자산과 생성 도구가 소유하고 제작 당시 통계·적용 위치·재생성·사람 확인은 `ART_01_환경에셋팩.md`가 기록한다. `16_AI_입체픽셀_스타일가이드.md`의 최종 미정은 이번 시험값으로 확정하지 않았다. 현재 상태는 `40_구현순서.md`, 새 문서 라우팅은 `00_시작.md`가 소유한다.

반경
새 실행 산출물은 PNG와 `.import` 각 4개, 재질 6개, 시각 씬 9개와 생성 도구 1개다. 기존 파일 수정은 `green_coast_m02.tscn`의 지면 재질 참조와 `m02_green_coast.gd`의 시각 생성·배치 부분, `docs/00_시작.md`, `docs/40_구현순서.md`, 이 장부에 한정했다. ART 기록 문서 한 개를 추가했다. 플레이어·M01/P1, NavMesh·충돌·카메라·저장 스키마·ID·비용·입력과 실제 사용자 세이브는 수정하지 않았다.

확인법
직접 확인: 기본형 삼각형은 바위 46, 절벽 36, 나무 72, 관목 36, 목책 60, 폐허 잔해 72, 벤치 60, 전시 받침 36, 목재 채집물 108, 돌 채집물 36이다. 텍스처 4개는 64×64, 재질은 6개이며 M02 NavMesh는 정점 112·폴리곤 116 그대로다. 일반 1280×720 Compatibility 렌더에서 거점·숲·폐허·전망을 실제 확인했다. 실제 물리 프레임 WASD, 직접 우클릭 엔진 이벤트, 절벽 전망, 격리 신규 저장의 획득·설치·전시·이어하기와 실제 사용자 저장의 읽기 전용 복사본 표시를 확인했다. 메인 M02와 비교용 M01·P1 장면도 오류 없이 열렸다. OS 실제 입력, 장시간 성능과 최종 스타일·재미는 사람 확인 전 통과 처리하지 않는다. 상태는 `ART-01 — 에셋 제작·지역 적용 완료·사람 확인 대기`; stage/commit/push하지 않는다.

### 023 — M03 첫 전투·위험·성장 루프

▶ 지시
[요약] 최신 로컬 M02의 거점→탐험→채집·기념품→귀환→거점 변화→저장 흐름 사이에 좌클릭 근접 공격, Space 회피, HP·피격·사망, 근접·원거리 적과 시험 엘리트, Monster Core 4개, 거점 회복·Prototype Blade 1회 강화, 전투 완료와 기존 저장 호환을 한 번에 연결하라는 사용자 지시. 새 지역·범용 전투 체계·아트 개선·외부 도구 없이 MUST를 먼저 실제 실행 검증하고 최초 구현에서는 stage/commit/push하지 않는다.

자리
`scenes/prototype/green_coast_m02.tscn`을 계속 F5 메인 장면으로 사용하고 `scripts/prototype/m03_green_coast.gd`가 기존 `m02_green_coast.gd`를 상속해 전투 지역·진행·저장 선택 필드를 추가한다. `m03_player.gd`는 기존 P1 이동을 상속해 회피·HP 피드백만 겹치고, `m03_enemy.gd`, `m03_projectile.gd`, `m03_health.gd`가 World 적·투사체·작은 체력 책임을 나눈다. 범위·수치·검증·사람 확인은 `docs/M03_첫전투성장루프.md`가 기록한다.

한 일
기존 Green Coast에 고정 Coast Crawler 2, Ruin Wisp 2, Ruin Guard 1을 배치했다. Crawler/Guard는 같은 근접 상태 기계와 export 성격의 설정을 재사용하고 Wisp만 거리 확보·비유도 투사체 공격을 사용한다. 공격은 카메라 월드 XZ 방향으로 1회 판정하고 벽 레이·사거리·방향을 확인하며 우클릭 목표를 취소한다. 회피는 CharacterBody 충돌 이동과 짧은 무적을 사용한다. 거점 Safe Zone, Spawn Leash/회복, 사망 뒤 안전 시작점·적 초기화·진행 무손실을 연결했다. Core 드롭은 기존 Interactables/E 흐름에 넣고 고정 작업대의 Core 4 차감과 무기 +1을 원자적으로 바꾼다. 기존 version 1 저장에 선택 키 세 개를 추가해 구세이브 누락 키를 기본값으로 받는다. 검토 에이전트 0.

바뀐 정본
M03 시험 구현은 새 M03 스크립트와 기존 Green Coast 실행 장면·입력 맵이 소유한다. 시험 조작·수치와 기술/사람 확인은 `M03_첫전투성장루프.md`, 상위 전투 관계는 `15_전투.md`, 최종 미정과 한정 선택은 `50_닫을것.md`, 현재 재개 지점은 `40_구현순서.md`가 소유한다. Q-013과 사망 규칙은 플레이테스트 중이며 최종 확정하지 않았다.

반경
새 영구 파일은 `m03_green_coast.gd`, `m03_player.gd`, `m03_enemy.gd`, `m03_projectile.gd`, `m03_health.gd`와 Godot UID, `M03_첫전투성장루프.md`다. 기존 수정은 `project.godot`의 전투 입력 두 개, `green_coast_m02.tscn`의 M03 스크립트·HUD 문구, `docs/00_시작.md`, `docs/15_전투.md`, `docs/40_구현순서.md`, `docs/50_닫을것.md`, 이 장부에 한정했다. M02 코드·NavMesh·P1 원본·카메라·ART-01 자산과 실제 사용자 저장을 변경하지 않았다. 진단 스크립트·격리 저장·렌더 캡처는 결과 확인 후 프로젝트에서 제거한다.

확인법
직접 확인: Crawler 2와 Wisp 2의 Core 합계 4, 기본 피해 16, 강화 피해 24, Guard HP 110을 5회 강화 공격으로 처치하고 전투 완료 저장까지 확인했다. 실제 Crawler 예고·근접 피해, Wisp AI 예고·투사체 피해, Safe Zone 복귀, 검의 폐허 벽 차단, 회피 무적·서쪽 경계 충돌, 좌클릭·메뉴·Vista 입력 우선순위를 물리 프레임에서 확인했다. 사망 뒤 HP 100·안전 시작점·적 Spawn 초기화와 진행 무손실, M02 채집·기념품·벤치·전시와 M03 세 필드의 동시 저장·계속하기, 새 키 없는 M02 저장 기본값을 격리 경로에서 확인했다. M03 플레이어의 WASD와 카메라 투영 우클릭 이벤트 이동, 1280×720 Compatibility 일반 렌더의 HUD·무기·적·예고·거점 소품을 확인했다. OS 실제 입력 감각, 전투 난이도·재미, 강화 체감과 장시간 플레이는 사용자 A/B/C 확인 전 통과 처리하지 않는다. 상태는 `M03 — 첫 전투·위험·성장 루프 구현완료·사람확인대기`; stage/commit/push하지 않는다.

### 024 — M05 시각 작업용 스킬·Blender 관찰 도구 준비

▶ 지시
[요약] M05 Visual Vertical Slice / 입체 픽셀 아트패스의 완성도를 높일 수 있는 skill과 플러그인을 조사하고 RPGpro에 적용하라는 사용자 지시.

자리
현재 진행표는 M03 사람 확인 대기이며 저장소에 M05 범위 문서는 아직 없다. 기존 아트 정본과 ART-01 결과, Blender MCP 연결·바위 제작 이력을 확인하고 게임 디자인이나 M05 완료 조건을 새로 정하지 않는 도구 준비만 수행했다.

한 일
Codex의 저장소 범위 스킬 위치를 사용해 `.agents/skills/rpgpro-visual-slice/`를 만들었다. 스킬은 승인 범위 확인, 기준 프레임 선정, Godot 우선 제작, 이미지 생성의 컨셉 한정 사용, 고가치 형태에만 Blender MCP 사용, 실제 Godot 화면 비교와 사람 판정 분리를 지시한다. 기존 프로젝트 전용 Blender MCP 1.9.1에는 장면·객체 조회에 더해 뷰포트 캡처 도구만 허용하고 호출 승인을 유지했다. `execute_blender_code`, 외부 에셋 다운로드, Meshy·Tripo·Hyper3D와 새 Godot MCP는 설치·활성화하지 않았다. 검토 에이전트 0.

바뀐 정본
반복 가능한 M05 시각 제작 절차는 `.agents/skills/rpgpro-visual-slice/SKILL.md`, 스킬 UI 표시는 같은 폴더의 `agents/openai.yaml`, Blender MCP 도구 허용은 `.codex/config.toml`, 현재 조각과 M05 미착수 상태는 `docs/40_구현순서.md`가 소유한다.

반경
새 파일은 저장소 범위 스킬 두 개다. 기존 수정은 `.codex/config.toml`의 관찰 도구 허용과 진행표·이 장부 기록에 한정했다. 게임 코드, 씬, 에셋, 렌더러, Blender 파일과 사용자 홈 전역 설정은 바꾸지 않았다. stage/commit/push하지 않는다.

확인법
직접 확인: Codex 공식 문서의 저장소 스킬 위치와 스킬 구조, 프로젝트에 이미 존재하는 Blender MCP 1.9.1·safe mode·텔레메트리 비활성·루프백 설정, 현재 세션의 장면·객체 조회 도구 노출을 대조했다. 새 스킬은 구조 검증을 수행하고, 뷰포트 캡처 도구의 실제 노출은 새 Codex 작업에서 확인한다. M05 게임 화면·자산은 만들거나 통과 처리하지 않았고 M03 및 ART-01의 사람 확인 대기 상태를 유지한다.

### 025 — 시각 제작 환경 복구 문서 검증·반영

▶ 지시
[요약] 지금까지 적용한 시각 제작 도구와 스킬을 실제 상태로 검증해 `01_환경복구.md`에 넣으라는 사용자 지시.

자리
`AGENTS.md`, 시작 라우터, 현재 진행표, 기존 환경복구 문서와 `rpgpro-visual-slice`를 다시 읽었다. Blender B0~B3 외부 보고는 경로와 이전 확인 근거를 찾는 데만 사용하고, 현재 파일·명령 결과를 우선해 재검증했다.

한 일
환경복구 문서에 Godot, Blender Portable, uv/uvx, MCP용 Python, Blender MCP와 저장소 범위 시각 스킬의 실제 경로·버전을 추가했다. 프로젝트 MCP의 루프백·safe mode·텔레메트리 차단·호출 승인·관찰 도구 목록, Blender 애드온과 bundled 1.9.1 파일 일치, B2/GLB 시험 자산 무결성, Blender 서버 시작과 새 Codex 작업 재로딩 순서를 기록했다. 코드 실행과 외부 생성 서비스는 복구 기본값에서 제외했다.

바뀐 정본
도구 경로·버전·복구 절차와 검증 상태는 `docs/01_환경복구.md`, 현재 조각을 바꾸지 않은 문서 작업 상태는 `docs/40_구현순서.md`, 실제 수행 이력은 이 장부가 소유한다.

반경
수정은 `docs/01_환경복구.md`, `docs/40_구현순서.md`, `docs/지시장부.md` 세 문서에 한정했다. 게임 코드·씬·에셋, Blender 파일·애드온, `.codex/config.toml`, 스킬 파일과 홈 전역 설정은 변경하지 않았다. stage/commit/push하지 않는다.

확인법
직접 확인: Godot `4.7.2.stable.official.ed1daf0bf`, Blender `4.5.13 LTS` build `daeeeca98fb0`, uv `0.12.10`, Python `3.11.16` 실행 성공. 현재 Godot 프로젝트는 정상 사용자 환경의 headless editor로 종료 코드 0에서 로드됐다. Blender 애드온과 보관된 bundled addon의 크기·SHA256이 일치하고, B3 GLB와 프로젝트 반입본도 크기·SHA256이 일치했다. MCP TOML 파싱과 안전·승인·관찰 도구 값, 전용 스킬의 현재 세션 발견과 공식 validator 통과를 확인했다. 현재 Blender 앱/애드온 서버가 꺼져 있어 실제 `get_scene_info`는 연결 실패했으며, `get_viewport_screenshot`의 세션 노출과 실시간 캡처는 새 작업·Blender 서버 시작 뒤 확인할 항목으로 남겼다. 저장소에는 기존 미커밋·미추적 작업이 있어 pull/reset/clean하지 않았고 이번 문서만 추가 수정했다.

### 026 — M04 첫 보스·희귀 보상·트로피·귀환 완결 루프

▶ 지시
[요약] 최신 로컬 M03 전투 기반 위에서 같은 Green Coast의 첫 Boss 장소, 2단계 Tidebound Guardian과 최대 세 패턴, Boss HUD, 사망·재도전, 최초 격파, 고유 Tide Crest·Tide Core, 거점 Trophy 전시, Blade +2, Save/Continue와 M02/M03 회귀를 한 묶음으로 구현하라는 사용자 지시. 새 Region·범용 Boss 체계·아트패스·외부 도구 없이 진행하고 최초 구현에서는 stage/commit/push하지 않는다.

자리
원격 `main`의 마지막 승인 묶음은 `adaeb6b5f7aabc4f0508b9f2a0ec836f154f543d`이며 DOC-STYLE-01·ART-01·M03와 M05 준비 문서의 기존 로컬 변경을 보존했다. `scenes/prototype/green_coast_m02.tscn`을 계속 F5 장면으로 사용하고 `m04_green_coast.gd`가 M03을 상속한다. M03 사람확인은 완료로 승격하지 않았다.

한 일
동쪽 폐허 너머에 기존 지역과 이어지는 작은 평면 Arena, M03 완료 전 접근 장벽, 전투 중 출구 장벽과 전용 목적지 범위를 추가했다. Tidebound Guardian은 기존 Health·Damage·Projectile을 재사용하며 HP 300, 정면 Sweep, 경고 원형 Slam, Phase 2 비유도 3발 Tide Burst를 작은 전용 상태 기계로 실행한다. 사망 시 공격·투사체·HUD·장벽을 정리하고 보스를 완전 초기화한다. 최초 격파와 수령을 분리 저장하고 보이는 E 보상으로 Tide Crest·Tide Core를 한 번 지급한다. 거점 고정 전시와 기존 작업대 Blade +2 피해 30, 첫 원정 완료 상태를 연결했다. 검토 에이전트 0.

바뀐 정본
M04 시험 구현은 `m04_green_coast.gd`, `m04_boss.gd`와 기존 Green Coast 장면이 소유한다. 시험값·기술/사람 확인은 `M04_첫보스_트로피_완결루프.md`, 상위 시험 관계는 `11_규칙_핵심루프.md`, `15_전투.md`, `31_아이템.md`, `33_지역.md`, `34_세이브.md`, 최종 미정 경계는 `50_닫을것.md`, 재개 지점은 `40_구현순서.md`가 소유한다. 이름·수치·패턴·보상 조합을 최종 규칙으로 승격하지 않았다.

반경
새 영구 파일은 `scripts/prototype/m04_green_coast.gd`, `scripts/prototype/m04_boss.gd`와 Godot UID, `docs/M04_첫보스_트로피_완결루프.md`다. 기존 수정은 Green Coast 장면의 root script, M03의 추가 근접 대상·최대 무기 단계·사망 전 확장 지점, M03 플레이어의 기본 비활성 목적지 범위, 지정된 최소 문서와 이 장부다. M02/P1 원본 장면·NavMesh 리소스·카메라·ART 자산·렌더러와 실제 사용자 저장은 바꾸지 않았다. 진단 스크립트·격리 저장·렌더 캡처는 확인 후 프로젝트에서 제거한다.

확인법
직접 확인: Arena 접근 잠금과 내비게이션 연결, 내부 우클릭과 전투 중 외부 목적지 거부, 자동 진입·HUD·장벽, Sweep 정면 20/뒤 비명중, Slam 경고 후 24, Phase 2 전환과 Tide Burst 3발을 검사했다. 사망 뒤 Boss HP 300·Phase 1·Dormant와 거점 복귀, 재진입, 격파·미수령 보상 저장·복원, E 보상·전시, Tide Core 1회 소비·Blade +2 피해 30, 완결 Continue를 격리 경로에서 확인했다. 새 키 없는 M03 저장과 M03 단독 이동·공격·사망 회귀, M02 노드·Vista 보존, 1280×720 Compatibility/ANGLE 실제 렌더를 확인했다. OS 실제 입력 감각, 난이도·재미, 패턴과 Trophy 시각 만족은 사용자 A/B/C 확인 전 통과 처리하지 않는다. 상태는 `M04 — 첫 보스·트로피·귀환 완결 루프 구현완료·사람확인대기`; stage/commit/push하지 않는다.

### 027 — M04 사망 종료·Arena 입구 사용자 보고 보완

▶ 지시
[요약] 보스에게 죽으면 게임 실행이 꺼지고, 보스 구역을 막는 문이 울타리보다 안쪽에 있으며 동쪽 울타리가 이어져 입구인지 알기 어렵다는 사용자 확인 보고.

자리
M04의 기존 보스·Arena 구조를 유지하며 `m04_green_coast.gd`의 사망 훅과 동쪽 입구 배치만 최소 수정했다. 새 지역·보스 패턴·저장 상태는 추가하지 않았다.

한 일
치명타가 보스의 Sweep/Slam 호출 안에서 발생할 때 같은 호출 스택에서 보스 상태·충돌·투사체를 초기화하던 경로를 다음 idle 단계로 분리하고 중복 `combat_died` 연결을 제거했다. M02 동쪽 울타리 중 입구와 겹친 두 칸을 숨기고 짧은 울타리로 양옆을 다시 이었으며, 청록 문틀을 추가하고 접근 잠금문과 전투 장벽을 모두 외곽 `X=14.82`로 옮겼다.

바뀐 정본
실제 사망 정리 순서와 입구 좌표·시각 배치는 `scripts/prototype/m04_green_coast.gd`, 사용자 보완 결과와 사람 재확인 항목은 `docs/M04_첫보스_트로피_완결루프.md`, 현재 재개 지점은 `docs/40_구현순서.md`, 수행 이력은 이 장부가 소유한다.

반경
게임 변경은 `scripts/prototype/m04_green_coast.gd` 한 파일이다. M04 기록·진행표·장부만 함께 갱신했다. 기존 Save, 보스 수치·패턴·보상, M03/M02/P1 코드, NavMesh와 렌더러는 바꾸지 않았다.

확인법
직접 확인: 보스 Slam 호출 안에서 치명타를 발생시켜 실행 장면이 유지되고 다음 프레임에 보스전·HUD·장벽이 정리되며 1.35초 뒤 플레이어가 HP 100으로 거점에 복귀하는 것을 확인했다. 입구 울타리 두 칸 개방, 양옆 짧은 울타리 연결, 문과 장벽의 외곽 동일 좌표, 청록 문틀을 자동 검사했고 일반 1280×720 Compatibility/ANGLE 화면에서 실제 개구부를 확인했다. 사람이 다시 볼 것: 실제 조작 중 사망 시 게임 창 유지·재도전, 외곽에서 입구와 문 위치의 가독성. stage/commit/push하지 않는다.

### 028 — 오픈형 연속 지형·고산 조망 보완

▶ 지시
[요약] 맵이 조각나 보이지 않도록 평평한 지형 기초를 먼저 만들고 바다·암반·평원·숲을 간단히 끼워 넣으며, 암반 지역은 솟은 벽이 아니라 아래로 꺼진 절벽인 고산으로 구성한다. 고산에서는 실제 지나온 마을과 다른 지형이 아래에 작게 보이는 오픈형 3D 월드 구도를 사용한다.

자리
기존 MACRO-01 콘텐츠·진행 플래그·Save 키와 Home/Region Gate 흐름은 유지했다. Region 2만 동쪽 X=52의 분리된 평지에서 Home 남쪽 Z=38, Y=8의 높은 대지로 옮겼다.

한 일
Godot 기본 BoxMesh와 기존 공유 재질로 Home 아래의 넓은 지형 기초, 평원 저지대, 서쪽 숲 지면과 수관 실루엣, 동쪽 암반 접근부, 북·동·서의 낮은 바다를 한 번 생성한다. Region 2 콘텐츠·NavMesh·목적지 범위·적·상호작용 좌표를 고산 위치에 맞췄다. 가장자리 충돌 메시를 숨기고 지표 Y=8에서 저지대 Y=0까지 내려가는 암벽 면을 만들었다. 공유 추적 카메라에는 기본 비활성 다층 높이 추적을 추가해 MACRO-01만 켰고, 고산 북쪽 끝 자동 전망은 실제 Home·저지대를 내려다본다.

반경
게임 변경은 `scripts/prototype/macro_01_game.gd`, `scripts/prototype/p1_camera_follow.gd`다. P1 카메라 기본값은 기존 고정 높이를 유지한다. Blender·외부 에셋·새 지형 생성 체계·저장 키는 추가하지 않았다.

확인법
정적 확인: 새 물 재질 경로 존재, 스크립트 괄호 균형, 중복 함수 없음, 고산 경계 X=-9.5..9.5/Z=30..46, 정상면 Y=8·절벽 하단 Y=0을 대조했다. 체크포인트 직전 Godot 4.7.2 headless editor 파싱과 메인 장면 4프레임 스모크는 종료 코드 0으로 통과했다. 사람이 볼 것: 평상 고산 카메라에서 실제 Home·평원·숲·바다가 작게 이어지는지, 북쪽 끝 자동 전망과 복귀, 절벽 낙차와 충돌, Region 2 이동·상호작용·귀환. 그래픽 F5 화면은 통과 처리하지 않는다.

### 029 — 정본 체크포인트·초기화 후 재개 준비

▶ 지시
[요약] 현재 정본을 선별해 원격 체크포인트로 백업하고, 컴퓨터 초기화 뒤 이어서 개발할 수 있는지 확인한 다음 오늘 작업을 종료한다.

자리
원격 `origin/main`과 로컬 `main`은 작업 시작 시 기존 M02 체크포인트 `adaeb6b5f7aabc4f0508b9f2a0ec836f154f543d`로 같았고 기존 미푸시 커밋은 없었다. 그 이후 로컬에는 DOC-STYLE-01·ART-01·M03·M04·M05/MACRO-01·PLAYTEST-01과 연속 지형 보완이 누적돼 있다.

정본 판단
현재 라우터가 연결하는 정본 문서, F5 실행에 참조되는 코드·씬·재질·텍스처·UID, 프로젝트 전용 `rpgpro-visual-slice` 스킬과 재생성 도구만 포함한다. `.codex/config.toml`, Godot 캐시·개인 Save, 외부 Blender 원본·설치 파일, 루트와 기록함의 과거 지시서·모델 설정은 제외한다. 구현 또는 기술 확인을 사람 확인 완료나 최종 게임 규칙으로 승격하지 않는다.

복구
`01_환경복구.md`에 공개 저장소 clone 주소, `main`, Godot 4.7.2 경로와 F5 실행 장면, 프로젝트 스킬, 로컬 재설정 대상과 최소 smoke check를 연결했다. 초기화 후 `00_시작.md`→`40_구현순서.md`를 읽고 PLAYTEST-01 실행 검증부터 재개한다. 실제 체크포인트 커밋 해시와 원격 대조 결과는 커밋 후 완료 보고에 남긴다.

### 030 — MASTER-02 안정화·제작 Skill·Visual Production Baseline

▶ 지시
[요약] 초기화 뒤 복구한 정본을 직접 대조하고, 최신 Bright & Warm Stylized Low-Poly Fantasy Adventure 3D 기준을 ART-01보다 우선해 정본화한다. 기존 상위 Visual 검증 Skill과 책임을 나눈 Modeling 및 Material/Texture Skill을 만들고, 세 환경 카테고리의 실제 Production Exemplar와 독립 Reference Area를 제작·검증한다. 주인공·몬스터는 디자인하지 않고 제공 시트의 느낌을 향후 참고로만 기록한다. 세 프로젝트 Skill은 이후 승인된 체크포인트에서 원격 보존하되 이번 작업에서는 stage/commit/push하지 않는다.

자리
정본은 복구된 `C:\Users\CKIRUser\RPGpro`의 `main`, HEAD `c6fff7ed9b793bcd0b029245fe59778b49550402`로 원격 `origin/main`과 일치하고 시작 시 작업 트리는 깨끗했다. 실제 F5 Main Scene은 `green_coast_m02.tscn`, root는 M02→M03→M04→MACRO-01 상속을 연결한다. 기존 M03/M04/MACRO/PLAYTEST의 사람 확인 미완료 상태를 유지했다. 최신 기준 이미지 15개는 `art/reference/`에 제공됐다.

한 일
`rpgpro-visual-slice`를 최신 저폴리 화면 조립·검증 책임으로 갱신하고 `rpgpro-lowpoly-modeling`, `rpgpro-material-texture`를 공식 Skill 생성·검증 절차로 추가했다. Modeling은 Tier A Godot 반복형, 승인된 Tier B Blender 고유 실루엣, 별도 승인 전 금지된 Tier C 외부 AI 3D와 scale·pivot·ground contact·collision·NavMesh·module 연결을 소유한다. Material Skill은 silhouette→geometry→base color→material separation→lighting→필요한 texture 순서, 공유 family, 비고정 해상도와 안정적 filtering을 소유한다. 세 Skill에 비밀정보·PC 절대경로·외부 설치 의존을 넣지 않았다.

실제 제작
결정적 Godot 생성 도구 `tools/master_02_build.gd`로 texture 없는 Color Block 공유 Material 14개, Terrain 5개, Nature 6개, Village/Ruin 7개를 새 `green_coast_prod_v01` 경로에 만들었다. 4m 연결 폭과 local Y=0 접지를 MASTER-02 시험값으로 사용하고 단순 collision을 시각 geometry와 분리했다. `master_02_production_baseline.tscn`에 ground/path, cliff straight/corner/ramp, tree/bush/grass/rock, fence/ruin/building과 sand/water/원경을 함께 조립했다. 기존 F5 장면, Save, ID, 입력, 전투, NavigationMesh에는 연결하지 않았다.

시각 정본
최신 Gameplay Visual Target과 Green Coast Layout Target을 최상위에 두고 Terrain, Nature, Village & Ruins 시트를 환경 생산 계층으로 정리했다. Character/Monster 시트는 향후 밝고 따뜻한 저폴리 감각·상대 스케일·Goblin Base 파생 참고일 뿐 이번 구현이나 최종 디자인이 아니다. ART-01 64×64/Nearest 결과는 삭제하지 않고 과거 기술 시험으로 내렸다. `art/.gdignore`로 기준 이미지를 Godot 런타임 import에서 제외했다.

반경
새 프로젝트 정본 후보는 두 Skill, 최신 기준 이미지와 `.gdignore`, 신규 Material·asset scene·Reference Area·생성 도구, `MASTER_02_Visual_Production_Baseline.md`다. 기존 수정은 Visual Slice Skill, `.gitignore`, `00_시작.md`, `01_환경복구.md`, `14_아트스타일.md`, `16_AI_입체픽셀_스타일가이드.md`, `40_구현순서.md`, `50_닫을것.md`와 이 장부다. 캐릭터·몬스터, 기존 게임 코드·씬·ART-01 실행 자산·Save는 변경하지 않았다.

확인법
직접 확인: 세 Skill이 `quick_validate.py`를 통과했다. 생성 도구는 Godot 4.7.2에서 Material 14, Terrain 5, Nature 6, Village/Ruin 7, Reference Area 1을 오류 없이 만들었다. headless editor import, Reference Area scene load와 기존 Main Scene 4프레임 smoke가 성공했다. 1280×720 Compatibility/ANGLE Reference Area 렌더를 실제 확인하고 첫 결과의 과노출·먼 구도를 팔레트·조명·카메라 보정 후 다시 확인했다. Intel HD Graphics 530 드라이버 품질 경고는 남는다. 실제 F5 전체 플레이, Save/Quit/Continue, 최종 시각 만족도·디자인·전투 재미는 사람 확인 전 통과 처리하지 않는다. stage/commit/push하지 않았다.

### 031 — MASTER-02R Visual Production Baseline 보정

▶ 지시
[요약] MASTER-02의 제작 파이프라인과 18개 환경 모듈 체계는 통과시키되 첫 Reference Area의 primitive 형태, 형광 팔레트, 과노출, 전시판형 구성과 먼 카메라는 미통과로 취급한다. 최신 Gameplay/Layout/환경 Production Sheet를 우선해 대표 Terrain/Nature/Village-Ruins를 실질 재모델링하고 작은 실제 Green Coast 구간, Production Overview와 Gameplay Validation 화면을 만든다. 캐릭터·몬스터·게임 기능·Meshy는 제외하고 최초 실행에서는 stage/commit/push하지 않는다.

자리
MASTER-02의 공유 Material 14, Terrain 5, Nature 6, Village/Ruin 7, 세 프로젝트 Skill과 기준 이미지를 재사용했다. 기존 Reference Area는 blockout 근거로만 남기고 MASTER-02R 사람 판정은 새 Gameplay Validation 화면을 우선한다. 기존 M03/M04/MACRO/PLAYTEST 사람 미확인 상태는 유지했다.

한 일
결정적 생성 도구 `master_02r_build.gd`로 기존 18개 scene을 같은 경로에서 재생성했다. 절벽은 큰 faceted 암벽 면, 나무는 taper trunk·방향성 branch·비대칭 canopy, 바위는 flat-base directional boulder, 폐허는 irregular stone mass와 두꺼운 voussoir arch, 건물은 stone base·heavy timber·brace·roof overhang, 목책은 post/rail 비례가 읽히도록 바꿨다. 팔레트와 조명을 함께 낮추고 water를 pure cyan에서 coastal blue-turquoise로 옮겼다. Reference Area는 House→Path→Nature→Ruin→Cliff→Sand/Sea의 실제 구간형으로 만들고 높은 Overview와 가까운 Gameplay 카메라를 분리했다. 첫 렌더 뒤 과노출·지면 겹침을, 두 번째 렌더 뒤 빈 판·해안 연결·카메라를 추가 보정했다. 반복 원인을 막도록 세 Skill에 primitive production gate, 관계형 팔레트 calibration, 두 카메라와 최소 1회 corrective render 규칙을 보완했다.

반경
새 영구 파일은 MASTER-02R 생성 도구, Green Coast base reference, Overview/Gameplay reference scene과 이 보정 문서다. 기존 MASTER-02 Material과 18개 환경 scene, 세 Skill, 아트/진행/라우터/장부 문서만 수정했다. 기존 게임 코드·F5 Main Scene·Save·combat·player·input·Region·Boss·캐릭터·몬스터·ART-01 자산은 수정하지 않았다. 렌더 이미지는 `.godot` 아래 사람 확인용 임시 결과이며 원격 정본 대상이 아니다.

확인법
직접 확인: Godot 4.7.2 생성 로그가 Material 14, Terrain 5, Nature 6, Village/Ruin 7, Reference view 2를 보고했다. 18 asset scene, 14 material, missing resource 0, Texture2D 참조 0, 분리 충돌 17개를 검사했다. 두 Reference view와 기존 Main Scene의 headless 4-frame load, 두 Reference view와 기존 Main Scene의 1280×720 Compatibility/ANGLE 실행이 오류 없이 종료됐다. 세 Skill은 공식 validator를 통과했고 절대 사용자 경로·secret 패턴을 찾지 못했다. 시각 합격, 실제 플레이·장시간 화면과 이후 Region 생산 승인 여부는 사람이 Gameplay Validation을 본 뒤 결정하며 아직 통과 처리하지 않았다. stage/commit/push하지 않았다.

### 032 — MASTER-03 Green Coast Production Build + Full Playtest

▶ 지시
[요약] MASTER-02R보다 최신 Gameplay/Layout/환경 sheet를 우선해 실제 Production 기준과 기존 18개 A/B/C audit를 만들고, Terrain/Nature/Village-Ruins/Material/Lighting/Water kit을 확장해 실제 Green Coast에 통합한다. M02/M03/M04/MACRO 기능, Collision/NavMesh, Save/Continue와 구세이브를 보존한 채 New Game→Boss→Region 2→Return→Save→Continue 전체 흐름을 점검하고 실제 Gameplay frame을 제공한다. 주인공·몬스터 디자인, 외부 AI 3D, Region 2 Production Art, stage/commit/push는 제외한다.

자리
로컬과 원격 시작 HEAD는 `c6fff7ed9b793bcd0b029245fe59778b49550402`로 같고 MASTER-02/02R 변경은 미커밋 상태였다. `master_03_build.gd`가 기존 `green_coast_prod_v01` family를 중복 없이 확장하고, `master_03_green_coast.gd`가 `macro_01_game.gd`를 상속해 실제 F5 장면에 환경 layer만 얹는다. 검증·캡처는 `master_03_verify.gd`, `master_03_capture.gd`가 소유한다.

한 일
기존 18개를 A 12/B 6/C 0으로 판정하고 B 6개를 edge/shoulder/foliage/blade로 실제 보강했다. Terrain 35, Nature 18, Village/Ruins 29, Material 22의 총 82 reusable scene을 생성했다. Home/Village, main loop/shortcut, Forest, Ruins/Secret, Cliff/Beach/Vista, Boss Arena/Watchtower를 기존 Collision과 gameplay 위치에 맞춰 실제 Green Coast에 배치했다. reusable scene의 단순 Collision은 유지하되 실제 layer에서는 중복 Collision을 껐다. 기존 ART-01 visual만 숨기고 기능 root·ID·NavMesh·Save schema는 바꾸지 않았다. 첫 live render에서 밝은 orange roof를 어두운 terracotta shared material로 한 번 보정했다. 세 프로젝트 Skill의 scale/pivot/collision, shared material, actual gameplay frame 기준을 적용했다.

반경
새 영구 파일은 MASTER-03 generator, runtime integration, verification, capture와 이 기록 문서다. 기존 scene 수정은 F5 root script 연결 한 줄이며, 기존 생성 Material/asset scene은 확장·재생성됐다. 지정된 router/환경/월드/아트/지역/진행/미정/M02R 문서를 갱신했다. 플레이어·적·보스 디자인과 로직, Save key, NavMesh, Region 2 Production Art는 변경하지 않았다.

확인법
직접 확인: Godot 4.7.2에서 kit 생성, editor parse, 메인 scene headless smoke를 통과했다. 총 82 scene·22 material·StaticBody3D 49개·missing `res://` 0개를 확인했다. 격리 Save 전체 자동 흐름은 WASD, destination, navigation, gather, secret, Vista, home display, enemies/Core/+1, boss death retry/defeat/reward/trophy/+2, Region 2 entry/craft/return, Save/Continue와 version 1 구세이브 허용의 32개 검사를 통과했다. 1280×720 Compatibility/ANGLE 실제 Home/Cliff/Ruins/Beach/Vista/Boss/Overview frame을 렌더하고 한 차례 palette correction을 반영했다. 실제 사람 키·마우스 체감, 장시간 성능·전투 재미와 최종 시각 만족은 통과 처리하지 않는다. stage/commit/push하지 않았다.
